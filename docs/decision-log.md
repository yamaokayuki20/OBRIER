# オブリエ 改善提案対応表

> 意思決定の記録。**「対応しない」と判断した機能を実装しないこと。**
> 元ファイル: `オブリエ_改善提案対応表.xlsx`

## 判断状況サマリー（対応表シート）

| 判断状況 | 件数 |
|---|---|
| 対応する | 16 |
| 対応しない | 17 |
| CSVエクスポートに対応 | 1 |

## 対応表

| No | 視点 | 提案 | 内容 | 関連する狙い | 期待効果 | 優先度(案) | 実装負荷(目安) | 対応判断 | メモ |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 取り込み・検出精度 | 暗黙の感謝の検出 | 「ありがとう」を含まない感謝表現（「助かった」「さすが」「おかげで」等）もLLMで検出して取り込む | ②／基盤 | 取りこぼしが減り、缶の中身が実態に近づく | 高 | 中 | 対応する |  |
| 2 | 取り込み・検出精度 | 誤検出の除外と学習 | カードを「これは感謝ではない」とワンタップで外せる。外した結果を検出器が学習する | 基盤 | 社交辞令ばかり届く不信感を防ぐ。検出品質の要 | 高 | 中 | 対応する |  |
| 3 | 取り込み・検出精度 | 対面・電話の感謝を手動記録 | チャット外でもらった感謝をその場で30秒で追加できる（音声入力対応） | ①② | AIの届かない無形の貢献も缶に入る | 中 | 低 | 対応しない |  |
| 4 | 取り込み・検出精度 | 過去ログの遡り取り込み | 初回連携時に過去1年分のチャットを解析し、開いた瞬間から数百通が入っている状態にする | ① | 「開けてみたら宝物はもう集まっていた」の初回体験をそのまま実装できる | 高 | 中 | 対応する |  |
| 5 | 取り込み・検出精度 | 絵文字リアクションの取り込み | 感謝系の絵文字リアクションを「小さなありがとう」として控えめにカウントする | ③ | 言葉にならない感謝も拾える | 中 | 低 | 対応する |  |
| 6 | 取り込み・検出精度 | 連携先の拡張 | Slack/Teamsに加えChatwork・メール・LINE WORKS等へ対応する | ビジネス | 対象ユーザーの拡大 | 低 | 高 | 対応しない |  |
| 7 | 取り込み・検出精度 | 皮肉・定型文のフィルタ | 皮肉表現や自動送信の定型文を感謝から除外する | 基盤 | 宝物の純度を守る | 中 | 中 | 対応する |  |
| 8 | 自己肯定感の体験 | 月に一度の「開封の儀」 | 月末に届いた感謝をまとめて開封する演出付きの振り返り体験。缶の蓋が開くアニメーション | ① | 儀式性が体験の核になり、サービスの記憶に残る | 高 | 中 | 対応する |  |
| 9 | 自己肯定感の体験 | 「今日は開ける日」ボタン | 落ち込んだ日に押すと、過去の言葉から特に響いたものを数枚選んで見せてくれる | ① | 情緒価値の中核。使い続ける理由が一番強い機能 | 高 | 中 | 対応しない | 落ち込んでいるかどうかはわからない |
| 10 | 自己肯定感の体験 | 節目の演出 | 100通目・初めての送り主・利用1周年などの節目をささやかに祝う | ① | 継続の小さな楽しみになる | 低 | 低 | 対応する |  |
| 11 | 自己肯定感の体験 | 年間アルバム | 1年分の感謝を1冊のアルバムに綴じる（PDF・印刷対応）。「オブリエ＝綴じる」の体現 | ① | 手元に残る形が愛着と口コミを生む | 中 | 中 | 対応する | そもそもアルバムみたいな画面デザインにしような |
| 12 | 自己肯定感の体験 | お気に入りピン留め | 特に効いた言葉を自分で選んで最上段に固定できる | ① | 自分だけの特効薬を常備できる | 中 | 低 | 対応しない |  |
| 13 | 得意の発見 | 月次「カタチレポート」 | レーダーの変化と代表エピソードをAIが文章で言語化した月次レポートを届ける | ② | 「自分の得意はこれ」と言えるようになる中核価値 | 高 | 中 | 対応する |  |
| 14 | 得意の発見 | エピソードのSTAR形式整理 | 経緯（状況→自分の行動→もらった感謝）を職務経歴書・面談用の実績形式でエクスポートできる | ② | 転職・1on1・評価面談でそのまま使える外向きの価値 | 高 | 中 | 対応する |  |
| 15 | 得意の発見 | カタチの経年比較 | 半年前・1年前の自分のカタチと重ねて表示する（他人とは比べない） | ② | 成長が形で見える | 中 | 低 | 対応する |  |
| 16 | 得意の発見 | 「意外な得意」の指摘 | 本人の自己認識アンケートと感謝の実態のギャップをAIが指摘する | ② | 思い込みの外にある強みに気づける | 中 | 中 | 対応しない |  |
| 17 | 得意の発見 | 感謝の広がりマップ | 誰から・どの部署から感謝が届いているかを可視化し、自分の影響範囲を見せる | ② | 自分の役割が組織の中の位置として見える | 中 | 中 | 対応する |  |
| 18 | 感謝の連鎖 | カードから「返す」導線 | 届いたカードからワンタップで相手にお礼・返礼の感謝を送れる | ③ | 受け取る→送るの循環を最短動線にする | 高 | 低 | 対応しない |  |
| 19 | 感謝の連鎖 | 「送った感謝」の箱 | 自分が送った感謝も別タブで見返せる | ③ | 送る側の喜びも資産になり、送る動機が生まれる | 中 | 低 | 対応する |  |
| 20 | 感謝の連鎖 | 感謝の気づきサジェスト | 「最近○○さんに助けられていませんか？」と控えめに気づきを促す（強制・催促はしない） | ③ | 連鎖の起点を増やす。お節介にならない設計が条件 | 低 | 中 | 対応しない |  |
| 21 | 感謝の連鎖 | チーム全体量のゆるやかな表示 | 個人比較はせず「チーム全体で今月○通」だけを共有する | ③ | チームの空気の変化を実感できる。非比較原則は死守 | 低 | 低 | 対応しない |  |
| 22 | プライバシー・心理的安全 | 「会社からは見えない」の明文化 | 個人所有・上司や人事は閲覧不可であることを規約とUI上で常に明示する | 基盤 | 監視ツール化への不安はサービスの生死に関わる | 高 | 低 | 対応しない |  |
| 23 | プライバシー・心理的安全 | 共有範囲を本人が選ぶ | カード単位・レポート単位で「これだけ1on1で見せる」を本人が選択できる | ②／基盤 | 個人所有と外向き活用の両立 | 高 | 中 | CSVエクスポートに対応 | csvエクスポートはあってもいい |
| 24 | プライバシー・心理的安全 | 退職後も持ち運べる | 転職してもデータを個人アカウントで持ち出せる（ポータビリティ） | 基盤 | 「感謝は個人の資産」という差別化軸の実装 | 中 | 中 | 対応しない | とりあえずは会社が導入する想定 |
| 25 | プライバシー・心理的安全 | 送り主側への配慮設計 | 他人の発言が本人の缶に保存されることへの同意・表示名の扱い等を整理する | 基盤 | 法務・倫理上の要検討事項。要件定義で必ず決める | 高 | 中 | 対応しない | 会社が導入する想定なので、問題ない想定 |
| 26 | プライバシー・心理的安全 | 解析範囲のオプトイン | どのチャンネル・DMを解析対象にするか本人が選ぶ。既定は公開チャンネルのみ | 基盤 | プライバシー最大の地雷への構え | 高 | 中 | 対応しない | 同上 |
| 27 | 継続・習慣化 | 週次ダイジェスト通知 | 「今週は3通とどきました」を週1回だけ通知する（通知しすぎない） | ① | 想起のリズムを作る。頻度控えめが世界観に合う | 高 | 低 | 対応する | メール通知は週1ならあり |
| 28 | 継続・習慣化 | ホーム画面ウィジェット | スマホのホーム画面に累計数と最新の一言を表示する | ① | 日常の視界に入り続ける | 低 | 中 | 対応しない |  |
| 29 | 継続・習慣化 | 言葉で届く通知 | 「上司さんから、あたたかい言葉がとどいています」のように数値でなく言葉で通知する | ① | 世界観を通知にまで浸透させる | 中 | 低 | 対応する | 通知の文言としてはあり |
| 30 | ビジネス・提供形態 | 個人課金と企業導入の二段構え | BtoC（個人の缶）を核に、企業にはチーム導入プランを用意する | ビジネス | 収益性と「個人のためのサービス」の両立方針 | 高 | ― | 対応する |  |
| 31 | ビジネス・提供形態 | 無料枠の設計 | 直近30日分は無料、全期間の振り返り・カタチ分析は有料などの線引き | ビジネス | ユーザー獲得と収益の設計 | 中 | ― | 対応しない | 一旦は誰でも使えるように |
| 32 | ビジネス・提供形態 | 企業には集計値のみ提供 | 企業向けレポートは匿名の全体傾向のみ。個人の缶の中身は渡さない | ビジネス／基盤 | 収益化しても信頼を裏切らない一線 | 高 | ― | 対応しない | 企業は個々人のありがとうアルバムを見れる。 |
| 33 | 将来拡張 | AI対話履歴からの称賛取り込み | ChatGPT等との対話履歴から、実績・称賛に相当する記録を取り込む | ② | AIと働く時代への拡張（キセキ構想との接続） | 低 | 高 | 対応しない |  |
| 34 | 将来拡張 | 多言語対応 | 感謝表現の多言語検出に対応する | ビジネス | 海外展開の布石 | 低 | 高 | 対応しない |  |

## 上司・関係者ニーズ

| No | ステークホルダー | やりたいこと | 見たい情報 | 画面イメージ（どんな画面で見えるか） | 優先度(案) | 対応判断 | メモ |
|---|---|---|---|---|---|---|---|
| 1 | 上司 | メンバーの見えない貢献を把握したい | メンバーごとの感謝の受信状況とその内容（リモートで見えない「誰が何で頼られているか」） | メンバー一覧ダッシュボード：メンバーカードに今月の感謝数＋ミニカタチ（レーダー）＋最新の一言 | 高 | 対応する |  |
| 2 | 上司 | メンバーのアルバムを見たい | 個々人のありがとうアルバム（言葉と経緯） | メンバー詳細画面：本人が見ているものと同じアルバムビューを閲覧できる | 高 | 対応する |  |
| 3 | 上司 | 1on1の準備をしたい | 前回1on1以降に届いた感謝と傾向の変化 | 1on1準備ビュー：期間指定のサマリー＋「この話を聞いてみては」の話題候補 | 高 | 対応しない |  |
| 4 | 上司 | 評価の根拠を集めたい | 評価期間中の感謝実績とSTAR形式のエピソード | 期間レポート：評価面談用に定性エピソードを整理して出力（PDF/CSV） | 高 | 対応する | その人の一定期間におけるGoodjobを定量的にまとめたペラ一のレポートを作成する（1月単位とか？）ことで、1on1にも対応できるのでは |
| 5 | 上司 | 褒める材料をタイムリーに得たい | チーム内で今週生まれた感謝のハイライト | 上司向け週次ダイジェスト：見逃していた良い行動を週1で知らせる | 中 | 対応しない |  |
| 6 | 上司 | メンバーの得意に合わせて仕事を振りたい | メンバーの得意（カタチ）の比較 | チーム得意マップ：メンバー全員のレーダーを並べて見られる | 中 | 対応する | メンバー詳細画面で分かればいいんじゃないかな |
| 7 | 上司 | チームの助け合いの構造を知りたい | 誰から誰へ感謝が流れているか | チーム相関図：感謝の流れのネットワークグラフ。矢印の太さ＝回数 | 中 | 対応しない |  |
| 8 | 上司 | 孤立・不調の兆候に気づきたい | 感謝の受信・送信が減っているメンバー | 変化アラート：前月比で大きく減った場合に上司にだけ通知（本人に見えない通知になるため運用は要検討） | 中 | 対応する | メンバー詳細画面で対応することになる。1on1用の月次レポートにアラートとして出す形でいいと思われる。 |
| 9 | 上司 | 異動直後にチームを早く把握したい | メンバー全員の得意と頼られどころ | チーム得意マップ＋メンバー紹介カード（新任上司のオンボーディング用途） | 低 | 対応しない |  |
| 10 | 上司 | 朝会や表彰で紹介する材料がほしい | 今月の印象的な感謝ハイライト | 月間ハイライト画面：そのまま会議で投影できるレイアウト | 低 | 対応しない |  |
| 11 | 人事・経営 | 部署ごとの組織状態を把握したい | 部署別の感謝量・推移・カタチの傾向 | 組織ダッシュボード：部署比較と時系列推移 | 中 | 対応しない | 後日対応かな |
| 12 | 人事・経営 | 埋もれた人材を発見したい | 感謝を多く受けているが評価・等級で目立たない社員 | 人材ハイライトレポート：抜擢・配置転換の参考情報 | 低 | 対応しない |  |
| 13 | 人事・経営 | 退職リスクの兆候を検知したい | 感謝の受発信の急減・孤立傾向 | 変化レポート：部署単位→個人単位はプライバシーと表裏のため慎重に | 低 | 対応しない |  |
| 14 | チームメンバー | 誰に何を頼ればいいか知りたい | 同僚の得意タグ・頼られどころ | チーム得意マップ（メンバー版）：閲覧のみ。頼り先がすぐわかる | 中 | 対応しない |  |
| 15 | チームメンバー | 新しく入った人が早く馴染みたい | チームの誰に何を聞けばいいか | ウェルカムビュー：新メンバー向けの「このチームの頼り先ガイド」 | 低 | 対応しない |  |

## 追加対応_v3

| No | 種別 | 内容 | 状況 | 対応判断 | 備考 |
|---|---|---|---|---|---|
| 1 | FB対応 | メンバー詳細：もらった/おくったの時系列推移（折れ線グラフ）を追加 | 対応済み |  | 6か月分の2系列折れ線。7月は集計中表記 |
| 2 | FB対応 | メンバー詳細：過去の月次レポートを履歴から閲覧可能に | 対応済み |  | 月別リストから開ける。デモはYUKIYAMA・高橋が2か月分 |
| 3 | FB対応 | 設計原則の変更：上司ビュー＝本人が見ている画面そのまま＋上司限定パネル（＋α） | 対応済み |  | 詳細画面を「上司メニュー（推移・レポート・アラート）」＋本人3タブ（アルバム/カタチ/おくった）の構成に刷新。上司限定部分には「本人には表示されません」ラベル |
| 4 | FB対応 | メンバー一覧：もらった数とおくった数を両方表示 | 対応済み |  |  |
| 5 | FB対応 | 全画面：五角形レーダーチャートを廃止 | 対応済み |  | 場面タグは固定5分類でなく自動生成・可変（例：YUKIYAMAは8タグ）とし、可変長の横バーで表現 |
| 6 | FB対応 | メンバー一覧：前月比を廃止し、月ごとの推移（折れ線スパークライン）を表示 | 対応済み |  | 受送信2系列のミニ折れ線 |
| 7 | FB対応 | メンバー一覧：カード形式からテーブル形式へ変更 | 対応済み |  | 行クリックで詳細へ。並びは名前順 |
| 8 | FB対応 | 月次レポートを「1on1の準備メモ」として再設計（リサーチ実施） | 対応済み |  | 構成＝①今月のようす②そのまま伝えられる称賛（引用＋事実＋伝え方例）③気にかけたいサイン④聞いてみたい問い⑤前回からの持ち越し。リサーチ根拠：1on1事前に必要なのは前回との継続性/コンディション変化/具体的称賛材料/対話を開く問い |
| 9 | 追加提案 | 1on1メモ機能：レポート内に上司がメモを書き、翌月レポートの「持ち越し」欄に自動表示 | 提案・判断待ち |  | 継続性の担保。今回は欄とボタンのみデモ実装 |
| 10 | 追加提案 | 1on1前日にそのメンバーの準備メモを上司へ自動送付（カレンダー連携） | 提案・判断待ち |  | レポートを見る習慣を仕組み化する |
| 11 | 追加提案 | メンバー一覧の並び替えは名前順固定を既定とし、数値ソートは設けない | 提案・判断待ち |  | ランキング化の防止。ソースは要判断 |
| 12 | 追加提案 | 場面タグの運用設計：類似タグの自動統合・表記ゆれの正規化・タグ上限 | 提案・判断待ち |  | タグ可変化に伴う品質管理 |
| 13 | 追加提案 | アラートの解消フォロー：翌月レポートに「先月のアラートのその後」を自動記載 | 提案・判断待ち |  | 言いっぱなしのアラートにしない |
| 14 | 追加提案 | 管理者向け：上司の閲覧・エクスポートの監査ログ画面 | 提案・判断待ち |  | 導入企業への説明材料になる |

## 追加対応_v4

| No | 種別 | 内容 | 状況 | 対応判断 | 備考 |
|---|---|---|---|---|---|
| 1 | FB対応 | 開封の儀を廃止 | 対応済み |  | 要件定義・設計書からも削除（F08はカード削除に差し替え） |
| 2 | FB対応 | 推移チャートを株チャート風に刷新（期間タブ：1週間/1か月/1年/3年/5年、横スクロール、最大5年分） | 対応済み |  | TradingView参考の簡易版 |
| 3 | FB対応 | チャートのホバー表示（縦クロスヘア＋その時点のもらった/おくった数）。通常時は数値ラベル非表示 | 対応済み |  |  |
| 4 | FB対応 | よく感謝される場面：もっと見るでTOP10まで展開。各場面を押すとアルバムのタグ絞り込みへ遷移 | 対応済み |  |  |
| 5 | FB対応 | アルバムにタグ絞り込み機能を追加 | 対応済み |  | 絞り込み中は解除チップ表示 |
| 6 | FB対応 | 経緯モーダルに「じぶんのメモ」追加（本人のみ閲覧。鍵アイコン＋ホバーで説明ポップ） | 対応済み |  | メモ付きカードには付箋の目印 |
| 7 | FB対応 | お気に入り登録＋お気に入り絞り込み | 対応済み |  | カード右上の星シール |
| 8 | FB対応 | 「これは感謝ではない」→「アルバムから削除」に変更。確認ダイアログ付き・復元不可 | 対応済み |  |  |
| 9 | FB対応 | 紙テクスチャ背景＋アルバムらしい遊び心を追加 | 対応済み |  | 実装＝タグ連動マスキングテープ／お気に入り星シール／メモ付箋／日付スタンプ風／月ラベルのテープ装飾／紙グレイン＋ドット台紙 |
| 10 | FB対応 | スマホ対応（レスポンシブ） | 対応済み |  | ヘッダータブの折返し・テーブル横スクロール・モーダル/チャートの幅調整 |
| 11 | FB対応 | メンバー一覧：最新のありがとう列を削除 | 対応済み |  |  |
| 12 | FB対応 | メンバー一覧：期間指定（既定1か月、3か月・1年）で受送信数を再集計 | 対応済み |  |  |
| 13 | FB対応 | メンバー一覧：ソート（もらった/おくった列）・名前検索・要確認絞り込み | 対応済み |  |  |
| 14 | FB対応 | メンバー詳細のカタチを本人画面と完全同一に（同一コンポーネント使用） | 対応済み |  | じぶんのメモのみ上司には非表示 |
| 15 | 運用 | 要件定義書・基本設計書・詳細設計書をv1.1に改版 | 対応済み |  | 以後、修正の都度アップデートする運用 |
| 16 | 追加提案 | チャートに「はじめて感謝をもらった日」など節目マーカーを控えめに表示 | 提案・判断待ち |  | 株チャートのイベントマーカーの応用 |
| 17 | 追加提案 | じぶんのメモを検索・一覧できる「メモ帳」ビュー | 提案・判断待ち |  | 内省の資産化 |
| 18 | 追加提案 | 削除の代替に「アーカイブ（非表示だが復元可）」を用意するか要検討 | 提案・判断待ち |  | 復元不可の仕様は誤操作リスクあり。プロダクト判断 |
| 19 | 追加提案 | タグ絞り込みとお気に入りのURLパラメータ化（共有・ブックマーク可能に） | 提案・判断待ち |  | 実装時の設計事項 |

## 追記：2026-07 デザイン確定と仕様変更

> 上の表は改変しない。以下は 2026-07-25 に確定した決定の記録である。
> 検証は `prototype/obrier-card-simple-v1.html`（A/B/C/D の比較ページ）で行った。カードデザインは同日中に D案（紙＋マステ・D-1）→ A案（ベタ塗り・D-18）→ **B案（紙＋タグ色・D-19）** と変わっている。**現在の正はB案**であり、D-1とD-18はいずれも履歴として残しているだけで無効である。

| No | 日付 | 種別 | 決定 | 内容・理由 |
|---|---|---|---|---|
| D-1 | 2026-07-25 | デザイン | ~~カードデザインはD案（紙＋マステ）を正とする~~ → **D-18・D-19で上書き（最終はB案）** | 当時の決定：白い紙のカードに場面タグ色のマスキングテープで留めた見た目。**同日中にD-18でA案（ベタ塗り）へ、さらにD-19でB案（紙＋タグ色）へ変更されたため、本項は無効。履歴として残す** |
| D-2 | 2026-07-25 | デザイン | 背景は白地×薄い青の方眼（グラフ用紙） | 方眼ノートにカードを貼った佇まいとする。クリーム地の紙テーマは検証ページの切替としてのみ残す |
| D-3 | 2026-07-25 | デザイン | ~~カードは±1°未満の静かな傾きと紙のグレインを持つ。テープの角度と位置はカードごとにゆらぐ~~ | **D-18・D-19により無効。** テープ・静的な傾き・紙のグレインはいずれも用いない。日付スタンプはD-17でかすれたゴム印に具体化され、これは維持している |
| D-4 | 2026-07-25 | デザイン | **感謝本文は明朝からゴシックに変更** | 手ざわりはマスキングテープ・紙の質感・静かな傾きで出す方針は変更なし。設計原則から明朝の指定を外す |
| D-5 | 2026-07-25 | デザイン | 場面タグの代表7色を確定 | スピード対応 #F08A24／資料品質 #3D85C8／調整力 #55B7BF／気配り #F2A0BC／後輩サポート #33915F／アイデア #F5C63C／情報共有 #E8502F。**タグ自体は可変**であり、これは代表的な7種への色割当にすぎない（固定分類に戻すものではない） |
| D-6 | 2026-07-25 | デザイン | アバターは全員 濃紺 #3A4A66 の1色に統一 | 人ごとの色分けはしない。場面タグの色と競合させないため |
| D-7 | 2026-07-25 | デザイン | カード上で奥行き（3D）を持たせるのはアバターのみ | 本文・タグ・日付はカードと同じ平面に置く。ホバーは紙らしい控えめな動き（傾きがまっすぐ直って数px浮く）とし、3Dチルトと光沢は比較用の旧案（A・B）にのみ残す |
| D-8 | 2026-07-25 | デザイン | 拡大詳細の文字色はアバターと同じ濃紺で統一 | ― |
| D-9 | 2026-07-25 | 画面 | **カードの裏面（めくり）は廃止** | カード押下で拡大詳細を直接開く |
| D-10 | 2026-07-25 | 画面 | カード→詳細の遷移を確定 | カードが移動しながら裏返り、中央で拡大して詳細になる（約350ms）。戻るときは逆再生。`prefers-reduced-motion` では遷移しない |
| D-11 | 2026-07-25 | 画面 | **お気に入り★をカード表面から廃止し、拡大詳細の中へ移動** | カード上では押しにくいため。アルバムのお気に入り絞り込みは維持する |
| D-12 | 2026-07-25 | 画面 | 拡大詳細の構成を確定 | 送り主・チャネル・日時／オブリエのメモ／やり取りの流れ／元のSlack・Teamsを開くボタン（外部リンクアイコン付き）／お気に入り／じぶんのメモ（本人のみ）／削除（確認必須・復元不可） |
| D-13 | 2026-07-25 | 画面 | 拡大詳細に出典（Slack/Teams）のタグは置かない | 出典は「Slackで開く」「Teamsで開く」ボタンの文言で示す |
| D-14 | 2026-07-25 | 画面 | **管理者画面を新設（S09 管理者設定）** | (a)連携設定（Slack/Teams接続・解析対象チャンネル・再連携）(b)チーム/メンバー管理（所属・上司の割当）(c)監査ログ（上司・管理者のアルバム閲覧記録）(d)検出品質モニタ（誤検出報告件数・信頼度しきい値0.80）。**管理者もじぶんのメモは閲覧不可** |
| D-15 | 2026-07-25 | 文言 | **キザな言い回しを避ける** | UIの操作フィードバックには「綴じました」ではなく「保存しました」を用いる。サービス名の由来としての「綴じる」は残してよい |
| D-16 | 2026-07-25 | 実装 | 既に「対応する」と決めていた未実装機能を今回実装する | F02 誤検出の除外と学習／F04 絵文字リアクションの計上（カタチ内に控えめに表示）／F09 節目演出（控えめな帯）／F13 エクスポート（CSV・STAR）／No.11 年間アルバム（PDF）／No.13 月次カタチレポート／No.15 カタチの経年比較（他人とは比較しない） |
| D-17 | 2026-07-25 | デザイン | 日付は**かすれたゴム印**とし、カードの見せ方を**中身の量で出し分ける**（②の一部はD-20・D-21で改訂） | ①日付スタンプ：feTurbulenceのノイズを `mask-image` に使いインクの乗りをまだらにする。枠線にもマスクがかかるため押しムラのある判子に見える。傾き（-0.8〜-3.4度）とかすれ具合（マスク位置）はカードごとに散らす。②表示バリエーション：感謝は一言のことも長文のこともあり、絵文字リアクションだけのこともあるため、どれもカードとして成立するよう量で見せ方を変える。~~リアクションのみ＝絵文字を大きく置き丈を詰めて控えめに~~（**D-21で取り下げ。リアクションはカードにしない**）／短い一言（14文字以下）＝本文を大きくし余白で受ける／長文（54文字以上）＝左寄せ・6行で止め「…続きは詳細で」を添え全文は拡大詳細で読む／中くらい＝中央揃え（既定）。分量による差は見せ方のみで、順位付けはしない。**なおD-20により、丈を詰める／伸ばす扱いは廃止し、高さは全枚固定として行数で受け止める** |
| D-18 | 2026-07-25 | デザイン | ~~正のカードデザインをD案（紙＋マステ）からA案（ベタ塗り）へ変更する~~ → **D-19で上書き（B案に変更）** | 当時の決定：場面タグの色でカード全面を塗り、文字はその面に白抜き（淡いイエロー＝アイデア、ピンク＝気配りのみ濃色インク）で乗せる。理由：一覧にしたとき色の面が効き、より豊かに見えるため。マスキングテープ・厚い紙テクスチャ・白い紙の地は廃止し、手ざわりはかすれた日付スタンプ（D-17）と、面にごく薄く残した紙目に託す。角丸18px、タグピルは白の半透明地＋インク色、NEWピルは白地＋タグ色を深くした文字。**維持するもの**：方眼の台紙／濃紺 #3A4A66 のアバター（56px・白リング）／奥行きを持たせるのはアバターのみ（translateZ 20px）／本文はゴシック（明朝は使わない）／ホバーは数px浮く／かすれた日付スタンプ／中身の量に応じた見せ方（stamp/short/mid/long）／拡大詳細の濃紺統一／カード→詳細の遷移350ms | **同日中にD-19でB案（紙＋タグ色）へ変更されたため、本項は無効。履歴として残す**
| D-19 | 2026-07-25 | デザイン | **正のカードデザインをB案（紙＋タグ色）へ変更する（D-1・D-18を上書き）** | 白い紙のカード（#fffdfa・角丸12px）の上端に場面タグ色の帯（border-top 7px）を置く。ベタ塗りもマスキングテープも用いない。本文はゴシック600/.06em/2.05/14.5px・中央揃え。中央にアバター（48px・白リング・濃紺）、その下に本文・送り主名、最下部にタグピルと日時の行を置く。カードに傾き・紙のグレイン・ベタ塗りの紙目は持たせない。タグピルは淡色地＋濃色文字、日付はかすれたゴム印（インクはタグ色の派生色）。**維持するもの**：方眼の台紙／濃紺 #3A4A66 のアバター／奥行きを持たせるのはアバターのみ（translateZ 20px）／ゴシック本文／かすれた日付スタンプ／拡大詳細の濃紺統一 |
| D-20 | 2026-07-25 | デザイン | **カードの丈を全枚そろえる（固定268px）** | 中身の量で丈がばらつくと一覧の並びが崩れるため、高さは固定し、量の違いは行数で受け止める。短い一言＝17px・3行まで／中くらい＝14.5px・4行まで／長文＝13px左寄せ・5行で止めて「…続きは詳細で」。**拡大詳細では省略せず全文を表示し、長ければ詳細の中でスクロールする** |
| D-21 | 2026-07-25 | 画面 | **絵文字リアクションをカードにしない（D-17の該当部分を取り下げ）** | D-17で決めた「リアクションのみのカード」は取り下げる。アルバムにはリアクションのカードを出さず、F04はカタチ画面での件数表示のみに戻す。したがってカードの種別は短い一言／中くらい／長文の**3種**となる |
| D-22 | 2026-07-25 | 実装 | **カード→拡大詳細の遷移を作り直す** | 従来はカードの複製と詳細シートの2枚を90°で入れ替えていたため、瞬間的に2枚に見えることがあった。**表にカード・裏に詳細を貼った1つの要素ごと回して裏返す**方式に変更する。要素は常に1つなので、入れ替わりの隙間も二重の重なりも原理的に起こらない。拡大はこの要素の scale を「カード幅÷詳細幅」から1へ動かして表現し、表面には逆数の scale をコマごとに焼き込んで見かけの大きさを保つ。380ms・等速・24コマ。閉じるときは同じ動きを逆再生する |
| D-23 | 2026-07-25 | 実装 | **日付スタンプのかすれ（ノイズマスク）を取りやめる** | D-17で入れた feTurbulence の `mask-image` は、カードの枚数だけ再描画が走り描画が重かったため取りやめる。判子らしさは、タグ色を深めた濃色インク・薄い枠・カードごとに散らした傾きで出す。**D-17の①（かすれ）は無効、傾きの散らしは維持** |
| D-24 | 2026-07-31 | 実装 | **詳細から戻り切る最後の1コマで、カードが元の位置へ着かず跳ねていたのを直す** | 閉じる動きは開くときのアニメーションの逆再生だが、逆再生は 0ms 側で終わる。そこはアニメーションの「開始前」にあたり、`fill: forwards` では値が当たらないため、最後の1コマだけ変形が外れてカードが詳細の位置へ跳ね上がってから消えていた。`fill: both` に変更。跳ぶ量は元のカードが画面中央から遠いほど大きく183〜489px。**コマ落ちは1つも起きないため、フレーム時間の計測では検出できない**。自己診断に「着地ずれ」（戻り切った最後のコマと元のカードの位置の差／2px以下で合格）を追加した |
| D-25 | 2026-07-31 | 実装 | **合成の層に上げるのは動いているカードだけにする** | 3Dの舞台（`preserve-3d`）と `will-change` を全カードに常時与えていたため、画面内の枚数ぶん合成の層が増えていた。詳細を閉じる間は背景の幕が薄れるので画面ぜんたいが描き直し対象になり、その層を毎コマ重ね直していた。ページを送って枚数が多いほど戻りが重くなる（画面内5枚で0ms、11枚で37ms）。静止中は平面として置き、変形も2Dで書く（`translateZ(0px)` を書くだけで層になるため）。層79→48、引っかかり37ms→6ms。**見た目は変わらない**（画素で比較した差はにじみ止めの効き方のみ） |
| D-26 | 2026-07-31 | デザイン | **押した場所から波紋を出す** | 紙にインクがにじむ見立てで、色は場面タグの色（`--cc`）を使う。押した瞬間に実カードは隠れて複製が飛ぶため、実カードだけに出すと一瞬で消える。押した位置と時刻を覚えておき、飛ぶ複製の側で途中から続きを再生する。複製を写すときは広がりかけた輪を取り除く（動きは複製されず、止まったまま焼き付いて残るため） |
| D-27 | 2026-07-31 | 実装 | **飛ぶ複製にも `kind-*` を渡す** | 本文の大きさ・行数・寄せは `kind-*` で決まるが、それが付いているのは `.card` のほうで、複製に写しているのは中の `.card3d` なので効いていなかった。押した瞬間だけ既定（中くらい）の字面に化けていた（長文は大きくなり、短文は小さくなる） |
| D-28 | 2026-07-31 | デザイン | **文字の大きさをデジタル庁デザインシステムの基準に合わせ、スマホ／PCで切り替える（D-19・D-20の寸法を改訂）** | 従来は9〜19pxが20段階に散らばり、UI文字の大半が10.5〜12.5pxで、スマホとPCで同一だった。日本語は同じpxでも欧文よりずっと密になる（欧文小文字はemの4〜5割、日本語はほぼ全面）ため、欧文向けの数値をそのまま使うと潰れる。**基準：本文16px以上／14pxは付随情報や領域制約時に限る／14px未満は原則不可（デジタル庁）**。行送りはJLReq（1.7倍を既定、1.5〜2.0倍が望ましい）。7段階のトークン（`--fs-note` 12 / `--fs-cap` 13.5 / `--fs-ui` 14.5 / `--fs-body` 16 / `--fs-lead` 17.5 / `--fs-head` 20）に整理し、640px以下で小さい側の段（note/cap/ui）を上げ見出しを下げる。**入力欄はスマホで16px固定**（16px未満だとiOS Safariが焦点時にページごと拡大するため。`maximum-scale` では止めない——利用者から拡大を奪うため）。拡大詳細は全文を読ませる面なので本文16pxを適用。これにともない**カードの丈を268px→320px**、長文の行数上限を5行→4行に改める（脚部のスタンプ・タグピルも育つため、従来の丈では行の途中で切れる） |
| D-29 | 2026-07-31 | デザイン | **日付スタンプを1.5倍にする** | 10px→16px（枠・余白も対応して拡大）。従来は小さすぎて読み取れなかった。タグピルも釣り合いのため `--fs-cap` に上げる |
| D-30 | 2026-07-31 | デザイン | **ホバー中の立体チルトと光沢を有効にする（D-7・原則2の一部を改訂）** | ユーザー指示による（「プロトタイプでは立体チルトがオフになってるので、ONにして」）。設定値は 傾き±6.5度・浮き17px・光沢の強さ.28。D-7で「3Dチルトと光沢は比較用の旧案にのみ残す」としていたが、現行プロトタイプではホバー時に有効になっている。**CLAUDE.md の「ドキュメントと現行プロトタイプで食い違う場合は現行プロトタイプを正とする」に従い、ドキュメント側を改める**。静止時はまっすぐ置き、奥行きを持つのはアバターのみ、という点は維持する。UI/UX監査（A-26）で原則違反として指摘されたが、これはユーザーの明示的な決定であり、指摘ではなく文書の未追随が原因だった |
| D-31 | 2026-07-31 | 実装 | **UI/UXの合否を機械で判定する仕組みを置く** | `tools/check.mjs`（回帰15項目：これまでの保証を守る）と `tools/uiux-gate.mjs`（UI/UX 13項目：コントラスト・当たり判定・キーボード・モーダルの作法・空状態・動きを減らす設定）。基準の出典は `docs/uiux-references.md`。監査報告書が「あるべき姿」を書くのに対し、こちらは同じ基準を毎回機械で確かめる。**直したつもりで直っていない／直したのに別を壊した、を防ぐのが目的**。色の判定は canvas に描いて画素を読む（`color-mix()` の計算値は `rgb()` 以外の書式で返ることがあり、正規表現では取りこぼす）。フォーカスの見え方は子孫まで含めて比べる（カードは `:focus-visible` で中の `.card3d` に影を付けるため） |
| D-32 | 2026-07-31 | 実装 | **タブは ARIA のタブUIではなく `nav` + `aria-current="page"` にする** | `div` でキーボードから触れなかったものを `button` 化するにあたっての判断。WAI-ARIA のタブUIは「Tab順に入るのは選択中の1枚だけ、他は矢印キー」（ローピング tabindex）が前提で、残り2枚が `tabindex="-1"` になる。これでは「見えている操作要素すべてにTabで到達できる」を満たせない。切り替え先も tabpanel ではなく見出し・件数ごと入れ替わる section であり、元から `<nav>` で囲まれた「行き先の選択」に近い。拡大詳細は焦点を中へ移し、積み方式で最上位だけが Tab を受け持つ（入れ子のダイアログがあるため） |
| D-33 | 2026-07-31 | デザイン | **薄い文字は透過ではなく実色で作る** | コントラスト比を基準（本文4.5:1／大きな文字3:1）に合わせる過程での方針。`opacity` や `rgba()` は下地によって実際の見え方が変わるため、狙った比率にできない。`--stage-ink-soft` を `rgba(44,51,69,.62)` → `#5f6473` など。**お気に入りの★は「装飾」ではなく「お気に入り済み」を伝える情報**なので、読める濃さにする（`#ffd66b` → `#8a6410`）。場面タグの代表7色と濃紺アバターは変更しない。当たり判定は**見た目を大きくせず余白だけで広げる**（スマホ44px／PC24px）。素のチェックボックスは padding で広げられないため `appearance:none` で作り直し、見える四角は15pxのまま当たり判定だけ広げた |
| D-34 | 2026-07-31 | 実装 | **画面に出る数字の出どころを推移データに一本化する** | 「264通」と大きく出ているのにカードは12枚、CSV書き出しが絞り込みを無視して常に「12件」、同一メンバーの件数が画面ごとに違う、といった食い違いが監査で多数出た。原因は数字を画面ごとに手書きしていたこと。`shape.monthly` を唯一の出どころに決め、アルバム・場面タグの内訳・送り主ごとの通数・年間アルバム・1on1準備メモをすべてそこから生成する。総数は533通、**30枚ずつのページング**（基本設計8章の30件単位）。並び順（新しい順／古い順）を実際に動くようにし、絞り込みの状態表示は select の1か所へ集約（チップは廃止。select 自身が状態表示を兼ねるため必ず二重になる）。**時系列の並び替えであって人どうしの順位づけではないので、非ランキング原則には抵触しない** |
| D-35 | 2026-07-31 | 画面 | **節目の帯を常設せず、いちばん新しい1件が降りてきて数秒で引っ込む形にする（F09・A-27・A-45）** | 従来は3本を無条件にアルバムの流れの中へ積んでいた。合計150px、スマホでは229px（画面の27%）を占め、**このサービスの主役（届いた感謝を見返すこと＝狙い①）が初画面から押し出されていた**（スマホの初画面ではカードが1枚も見えていなかった）。直し方は「小さくする」ではなく「流れの中に置かない」。①同時に出すのは1件まで（日付のいちばん新しいもの）②`position: fixed` で本文の上に重ね、アルバムの高さを取らない③降りてきて5.5秒で静かに引っ込む（読んでいる間＝ホバー中・焦点が中にある間は引っ込めない）④閉じた／見た節目は `localStorage` に覚えて出し直さない（A-45。実装ではサーバ側の `Milestone.表示済みフラグ` を使う）。⑤次に開いたときは、その次の節目を1件だけ出す。**知らせであって催促ではない**ので、数え上げも「あと◯通で節目」も出さない（原則11・参照資料で却下済み）。効果（実測）：初画面の1枚目のカードの位置は PC 537.5px→342px、スマホ 844px→459px |
| D-36 | 2026-07-31 | 画面 | **過去の節目への静かな入口をツールバーに1つ置く** | 帯を常設しないぶん、見逃した人・もう一度見たい人のために `これまでの節目` を置く。押すと日付つきで新しい順に並ぶだけのダイアログを開く。自己主張しないよう枠と地色を持たない文字リンクにし、色は `--navy`（白地で9.4:1）、当たり判定は見た目を変えず余白だけで PC 24px／スマホ 44px を確保した。ツールバーの既存の折り返しの空きに入るため、画面の高さは増えていない |
| D-37 | 2026-07-31 | デザイン | **遊び心として3件を採用する（`docs/uiux-references.md` 第2部より）** | 物差しは「気づいた人だけがニヤッとする」「毎日見ても飽きない」。**採用**：①節目の静かな帯（D-35と一体）②**「1年前の今日」への静かな導線**——同じ日付に届いたありがとうが**ある日だけ**1行のリンクが出て、押したときだけ開く。**無い日は何も出さない**（毎日「今日は無い」を突きつけないため。基準日は実際の今日。プロトタイプで別の日の出方を見るときだけ `?today=2026-09-15` を渡す）③**チェックが自分で描かれる確認演出**——じぶんのメモの「保存する」を押すと、線が0.3秒で描かれてから文字が出る（SVGの `stroke-dashoffset`）。じぶんのメモは本人だけの記録（原則8）で、保存できたことは静かに、でも確かに返したい。合わせて重複していたトーストは外した。**採らなかったもの**：段階的に立ち上がる表示（staggered reveal）＝カードは静止して並んでいてほしい面で、開くたびに30枚が順に動くと落ち着かない／「無かった日」を強調しない＝現行の推移チャートはすでに0の日を他の日と同じ淡さで置いており、直すところが無い／タグの「はじめて」の印・年間アルバム表紙＝難易度中で、タグ運用（未決事項）と年間PDFの体裁に踏み込むため今回は見送る。**入れないもの**（参照資料で却下済み）：紙吹雪・花火／ストリーク／達成バッジ／効果音／SNSシェア／「あと◯枚で節目」／未取得項目のシルエット |
| D-38 | 2026-07-31 | 実装 | **デモ用の操作は `?demo=1` のときだけ出す（A-34）** | 「▶ 新着デモを再生」が右下に常設され、390pxではカード本文の上に重なっていた。本番には無い操作なので既定では出さない。あわせて**アルバムの説明文を1〜2行に縮め**（A-27。数の基準＝いつからいつまでのぶんかは残す）、スマホでは見出しと数を1行に置き、帯を外したぶんアルバムの頭の余白を詰めた。**初画面で見えるカード（実測）**：PC 1280×900 は全体が見えるカード3枚＋次の行3枚（前は3枚＋18px）、スマホ 390×844 は0枚→1枚が全体＋2枚目の上端41px。スマホでカード2枚を丸ごと収めることは、ヘッダー118px＋ツールバー152px＋カード320pxの合計から**算数の上で不可能**であり、これ以上の改善はヘッダーの圧縮（A-28・別課題）を待つ |
| D-39 | 2026-07-31 | 仕様 | **上司が、どのメンバーのカードでも拡大詳細を開けるようにする（A-19）** | これまで開けたのは YUKIYAMA さんのカードだけで、ほかのメンバーは `clickable:false` の静止カードだった。要件F16・画面一覧S05 は拡大詳細を「本人／**上司**」と定めており、経緯が読めないと1on1の準備という上司ビューの目的そのものが果たせない。全メンバーを `BOSS_OPTS`（押して開ける／じぶんのメモ・削除・誤検出報告・お気に入りは持たない）で描く。**じぶんのメモの欄には「本人だけのものです。上司・管理者には表示されません」が出る**（原則8は実装のまま維持され、`tools/check.mjs` の漏れ検査も通る）。手書きのデモカード5枚にも `summary` と `story` を足し、開いたときに経緯が読めるようにした。開いた記録は既存の `logAudit("カード詳細閲覧", …)` に残る |
| D-40 | 2026-07-31 | 仕様 | **上司の割当と権限の変更は、確認を経てから保存する（A-22）** | 従来は select を動かした瞬間に反映され、監査ログにも書かれていた。「本人」を「管理者」に変えても何も聞かれない。確認では**その変更で誰が何を見られるようになるか**を書く（例：「上司さんは、高橋さんのアルバムと月次レポートを閲覧できるようになります。じぶんのメモは、この変更後も表示されません」）。やめたとき（「やめる」・✕・Escape・背景を押す）は select を元の値へ戻す。トーストは対象を含める形に改め（「高橋さんの権限を保存しました」）、監査ログには変更の前後（「本人 → 管理者」）を残す。各 select に `aria-label`、`th` に `scope="col"` を付けた |
| D-41 | 2026-07-31 | 仕様 | **監査ログに絞り込み・件数表示・ページ送りを置く（A-23）** | 要件F23は「一覧・**絞り込み**して閲覧する」、基本設計は「1年間保持」と定めているのに、絞り込みもページ送りも無かった。条件は4つ（期間の開始日・終了日／操作した人／操作の種類／対象）、1ページ50件、件数は「全 186件 のうち、絞り込み後 21件（操作：カタチ閲覧） 中 1〜21件目を表示しています」の形で表の**前**に出す。CSVの書き出しは絞り込み後の範囲に従う（A-08と同じ考え方）。プロトタイプの記録が5件では絞り込みが意味を持たないため、過去1年ぶんを機械で用意した（180件）。この場で追記された行には「このセッション」の印を付ける（A-44） |
| D-42 | 2026-07-31 | 仕様 | **上司の閲覧記録を、カタチ・おくったの切替でも残す（A-46）** | 従来はメンバー詳細を開いた1回だけだった。監査ログのサンプルには「カタチ閲覧」の行があり、仕様としては最初から想定されている。サブタブの切替ごとに「アルバム閲覧／カタチ閲覧／おくった閲覧」を記録する（詳細を開いた1回目は二重に書かない） |
| D-43 | 2026-07-31 | 実装 | **入場アニメの遅れに上限を置く（A-25）** | `60 + n*40` で枚数に比例していたため、120枚では最後のカードが動き出すのが 4,820ms。要件の「アルバム一覧の初期表示1.5秒以内」を、枚数が増えるほど大きく割っていた。上限 420ms を置き、**枚数によらず**最後の1枚が 420ms には動き出すようにした（実測：120枚を一度に描いても1.5秒後に不可視のカードは0枚）。先頭から順に現れる見え方は、上限に届くまでの間そのまま残る |
| D-44 | 2026-07-31 | デザイン | **見出しの階層を、意味と見た目の両方で成り立たせる（A-30）** | `h1` が16px（本文と同寸・薄いグレー）で、見出しに見えていた `.lead-line` は22pxの `div` だった。目に見える順と支援技術に伝わる順が逆。基本設計2.3.1で定義済みだが使われていなかった `--fs-head`（20px／スマホ19px）を `h1` に、`--fs-lead`（17.5px）を `h2` に当て、`.lead-line` を `h2` に変えた。ダイアログ・拡大詳細の題も `h2` にして、中の①〜⑤（`h3`）に親を与えた。年間アルバムの表紙の題は画面の見出しではないので `h4` から `p` に外した。**階層は大きさと太さで作り、薄い色では作らない** |
| D-45 | 2026-07-31 | アクセシビリティ | **ラベルの無い入力をなくし、本文へのスキップリンクを置く（A-31・A-38）** | 入力4件（場面・並び順・期間・カタチレポートの月）に名前が無かった。接頭辞（「場面：すべて」「並び順：新しい順」）を見えるラベルとして残したまま、同じ語を `label` で読み上げ名として渡す。接頭辞を置けない検索欄・日付には見えるラベルを左に添え、プレースホルダは例示に回す。**選択肢ひとつひとつに「場面：」を付ける案は採らなかった**——select の幅はいちばん長い選択肢で決まるため、スマホでツールバーが1行増え（152px → 205px）、カードがさらに下へ押し出されたため。スキップリンクは画面の外に置き、焦点が当たったときだけ現れる。`width:1px` で潰す作りにはしない（当たり判定44pxを機械で確かめられなくなるため） |
| D-46 | 2026-07-31 | 画面 | **モーダルの作法をそろえる：暗幕・背景の固定・主要アクションの位置（A-13・A-15・A-16）** | ①暗幕を持っていたのは拡大詳細だけで、CSV・年間アルバム・STAR・削除確認・1on1準備メモ・監査ログCSVの6種は背景がくっきり見えたまま浮いていた。同じ濃さ（52%・200ms）の幕を全ダイアログに入れる。②開いたままホイールを回すと背景が動いていた（実測 scrollY 1388→1478）。`html { overflow: hidden }` で止める。`body { position: fixed }` 方式は幅の計算がやり直しになりカードが組み直されるため使わない（カードが元の位置へ戻る計算は実測値なので、1pxでも動くと着地で跳ねる）。消えるスクロールバーのぶんはその場で測って余白で埋め合わせる（`scrollbar-gutter: stable` を常時効かせる案は、狭い画面でも15px取られて本文の行数が増えるため不採用）。**閉じる裏返り（380ms）の間は解かない**——途中で幕の幅が変わると戻っているカードが横へ飛ぶ。③1on1準備メモの「1on1メモを書く」「PDFで書き出す」が1280×900で画面外だったので、ダイアログ下端に `sticky` で貼り付け、上端にぼかしを置いて続きがあることを示す |
| D-47 | 2026-07-31 | デザイン | **ヘッダーと節目の帯を不透明にする（A-28・ユーザー指摘）** | ヘッダーは `rgba(255,255,255,.82)` で、スクロールでその裏を通る月の見出し・カードの帯・アバターがタブの文字と重なって両方とも読めなかった。`backdrop-filter` は毎コマ背景を読み直すので使わず、素直に不透明（`#fff`＋1pxの境界＋ごく薄い影）にする。節目の帯は地色そのものは元から不透明だったが、幅620pxの「浮いた紙」だったため下端で本文が半分だけ覗き、文字が途中で切れて透けているように見えていた。**置き場（`.ms-host`）ごと画面幅いっぱいの不透明な帯**にしてヘッダーの直下に貼り付け、出入りは `transform` だけで動かす（`opacity` で薄くしない）。帯のすぐ下には24pxのぼかしを足し、切るのではなく消えていく形にした。引っ込むときはヘッダー（z-index 30）の裏へ滑り込む |
| D-48 | 2026-07-31 | 文言 | **完了の文言を「（対象）を（動詞）ました」にそろえ、モックの断り書きを1か所にまとめる（A-42・A-48）** | 「カードを削除しました」と「報告を受け取りました。このカードを外しました」のように、同じ結果に対して語彙も文の数も違っていた。型を決めて全トーストを書き直す（「このカードを削除しました」「このカードを、感謝ではないと報告しました」「YUKIYAMAさんの権限を保存しました」）。「（モック）」は11箇所に手で書かれていて実装時に消し忘れる形だったので、`mockToast()` 1か所に集めた（実装では `toast` に替えるだけで済む） |
| D-49 | 2026-07-31 | 画面 | **スマホの初画面にカードを2枚まるごと収めることは、算数の上でできない（現状維持）** | 844pxの画面にカード2枚（320×2＋間隔24＝664px）を収めるには、その上のすべてを **180px** に収める必要がある。ヘッダーだけで **113px**——当たり判定44pxの帯が2段（1段目：ロゴ・ロール切替、2段目：画面のタブ3つ）で、390px幅では横並びにできず段は減らせない。残る67pxに、届いた通数（「533通」＝40px）、期間の断り書き（2行57px）、絞り込み・並び替え・書き出しのツールバー（3行152px）、月の見出し（19px）が入らない。**見出しと説明を全部消し、ツールバーを1行に削っても 218px** で、なお180pxには届かない。今回できたのはヘッダーの余白を詰めた **6px**（1枚目のカードの上端 459px → 453px、ヘッダー 119px → 113px）まで。これ以上は、この画面から数か絞り込みか書き出しのどれかを取り上げることになるので**行わない** |

| D-50 | 2026-07-31 | 画面 | **節目の帯は「何も無い場所」に出す。押し下げも、覆いもしない（再監査 N-01）** | D-35 で「流れの中に置かない（高さを取らない）」ところまでは直したが、ヘッダーの直下に画面幅いっぱいで降ろしたため、390pxでは `h1`・「533通」・説明文を5.5秒のあいだ**覆っていた**。押し下げていたものを、こんどは隠していたことになる（主役が読めない点では同じ）。**本文に余白を足して押し下げる直し方は採らない**——D-35 で外したものを戻すことになり、スマホの初画面からカードがまた消える。置き場のほうを変える。**900px以上＝見出しの右の余白**（`h1` と `.count-wrap` の箱を文字の幅にそろえたので右側は空いており、**何も覆わない**）／**900px未満＝画面の下端に貼り付く帯**（余白が無いので重ねるほかなく、重ねてよいのは、すでに画面の縁で切れているカードの上端だけ。ここはこの画面がもともと知らせを出している場所＝トーストと同じでもある。浮かせず下端に密着させる——12px浮かせるだけで1枚目のカードの脚（場面タグと日付）に掛かる）。**初画面のカード（実測・変化なし）**：PC 1280×900 で全体が見えるカード3枚＋一部6枚、スマホ 390×844 で1枚全体＋2枚目の上端47px。D-47（帯を画面幅の不透明な帯にする）は「覆っている以上、下をきれいに隠す」ための策だったので、覆わなくなったここで役目を終える |
| D-51 | 2026-07-31 | デザイン | **見出しの寸法を本文との比で決め直す（再監査 VD-1・D-44 を改訂）** | `h2` 17.5px は本文16pxの**1.09倍**しかなく、大きさで階層が読み取れていなかった（25件）。`--fs-h2` 20px（本文の1.25倍）・`--fs-h1` 24px（1.5倍）を置く。**ただ大きくはしない**——上げ幅は「本文との関係が成り立つ最小限」に留め、アルバムの静かな佇まいを崩さない。`--fs-head`（20px）とは別のトークンにする：`--fs-head` は短い一言のカード本文にも使われており、そちらは D-28 で決めた字面なので動かせないため。**スマホの `h1` は 20px**（21px以上にすると「あなたに届いたありがとう」と「533通」が1行に収まらず、折り返したぶん主役のカードが30px下がる。実測で初画面の2枚目が 47px → 19px になった）。あわせてメンバー詳細（S06）の `.detail-name` を `div` から `h1` に変え、「各画面に h1 がちょうど1つ」を満たす（VD-2） |
| D-52 | 2026-07-31 | アクセシビリティ | **ダイアログの中の文字色を基準に合わせ、当たり判定の取りこぼしを埋める（再監査 A-03・A-11 の残り）** | D-33 で直したのは画面の側だけで、**ダイアログの中の色トークンは触れていなかった**（`--navy-soft #6d7a94` が白地4.32:1／パネル地3.92:1 のまま9種のダイアログで32箇所）。トークン1つを `#5c6884` に濃くして片づける（白地5.57:1／`#f2f4f8` 地5.06:1）。**濃紺 `--navy #3A4A66`（アバターと拡大詳細の文字色）は変えない**——役目の違う色なので統一は崩さない。プレースホルダの `opacity:.75` も外す（薄めると濃くした色でも3.3:1まで落ちる）。当たり判定は、`.back-link`（169×19）に PC 24px／スマホ 44px を**見た目を変えず余白だけで**与え、640pxの44px化から漏れていた `.mini-btn` `.danger-btn` `.sheet-close` `.chart-tab` `.sub-tab` `.adm-select` `.report-item` を加える。`.sheet-close` の丸い面は 32px のままにし、地色を `background-clip: content-box` で内側だけに塗って**押せる範囲だけ**を44pxにした |
| D-53 | 2026-07-31 | アクセシビリティ | **開いている間、背景を読み上げからも外す（再監査 A-02 の残り）** | 焦点は閉じ込め済み・暗幕でマウスも届かないが、読み上げのブラウズモードでは背後のカード533枚もヘッダーも読めていた。`inert` を body 直下の兄弟（開いている `.overlay` と知らせの器 `.toast-live` / `.toast` を除く）に付ける。`aria-hidden` は別に足さない（焦点が当たったまま隠す事故を避けるため、`inert` に一本化する）。判断は「幕がDOMに1つでもあるか」で、`syncScrollLock` と同じ拍で行う。**焦点を中へ移したあとに付ける**ので、背景に焦点が残ったまま不活性になることはなく、Tab の閉じ込め（keydown の capture）にも触れていない |
| D-54 | 2026-07-31 | アクセシビリティ | **カードの読み上げ名に、目で見て分かることを入れる（再監査 A-39）** | 読み上げ名は「（送り主）からの「（本文）」の詳細を開く」だけで、NEWの印・お気に入りの★・メモの付箋・場面タグ・受け取った日時が**一つも伝わっていなかった**。「（送り主）からの「（本文）」。場面：（タグ）、（日時）、（新着／お気に入り／じぶんのメモあり）。詳細を開く」に改める。冗長にしないため、**画面に出ている印と同じものを同じ順で**並べるに留める。★と付箋は `opts` で描き分けているので読み上げ名も同じ条件で作る——**上司には、じぶんのメモがあること自体を伝えない**（原則8） |
| D-55 | 2026-07-31 | 実装 | **ゲートが上司のメンバー詳細と1on1準備メモに到達できていなかったのを直す（再監査 N-02 の残り）** | 巡回に足したはずの2画面へ、実際には一度も入れていなかった。`.tbl tbody tr` の1件目が、先に描かれている「表で見る」の隠れた表（`#viewShape .chart-table`）の行に当たり、`element is not visible` で止まっていた。1on1準備メモも「1on1」という文字のボタンが無く開けていない（開くのは月次レポートの行 `.report-item`）。`#viewTeam .tbl tbody tr` と `.report-item` に直した。**測っていない場所は直っていないのと同じ**なので、巡回を足したときは「実際にその画面へ入れたか」まで確かめる |
| D-56 | 2026-07-31 | アクセシビリティ | **カードの★・付箋・読み上げ名を、いつも同じ1本の条件から作る（最終監査 N3-2）** | 読み上げ名（D-54）は `buildCard` で作られたきりで、状態の変化に追随していなかった。拡大詳細で★を付けても名前は「新着。詳細を開く」のまま、じぶんのメモを保存しても付箋（`.memo-dot`）が出ない（次に絞り込むまで現れない）。**画面には★が見えているのに読み上げでは何も変わらない**——何も言わないより、違うことを言うほうが困る。名前の組み立てを `cardLabel(d, opts)` に切り出し、`syncCardMarks(el, d, opts)` が★・付箋・名前の3つを**同じ `opts` から同時に**作り直す。★の切り替え（`[data-fav]`）とメモの保存（`commitMemo`：保存ボタン・入力が止まって1.5秒・閉じるときの確定、いずれの道も通る）で呼ぶ。飛んでいる複製（`.f-front`）にも同じことをするが、複製は `aria-hidden` なので名前は持たず見た目だけそろえる。**原則8の守り方は変えていない**：`opts.fav` / `opts.memo` は本人ビューでしか真にならないので、上司・管理者には★も付箋も出ず、「じぶんのメモあり」という語が生成されようがない。**印を増やすときは名前も同じ条件で作る**という規律を、関数1本に固定したことになる（分岐で書き足さないこと）。上司6メンバー×3サブタブ×各カードの拡大詳細＋1on1準備メモ＋管理者4タブの**78場面**を、533枚すべてに検査用のメモと★を入れた状態で総当たりし、読み上げ名の「メモ」「お気に入り」・付箋・メモ欄・秘密文字列（本文／`aria-label` `title` `placeholder`／`aria-labelledby` `describedby` の参照先／入力欄の値）のいずれも**0件**であることを確認した |
| D-57 | 2026-07-31 | アクセシビリティ | **入れ子のときは、いちばん上の器以外すべてを読み上げから外す（最終監査 N3-4）** | D-53 の判断は「幕がDOMに1つでもあれば、最初に外した顔ぶれのまま」だったため、削除の確認を開いている間、下段の拡大詳細（312文字）が仮想カーソルで読めていた。見えている面と読める面が食い違う点は、背景のカード533枚を読めていたのと同じである。`syncBackgroundInert()` を「body の直下の兄弟のうち、**いちばん上の `.overlay` 以外すべて**（知らせの器は除く）」に改めた。Tab は元から下段へ落ちないので、変わるのは読み上げの範囲だけ。**閉じる順（積みから外す → 器を取り除く）は変えていない**——下段が `inert` のまま焦点を返そうとする瞬間があるが、`popModalFocus` の「戻し先が `[inert]` の中にある間は待つ」（N3-1 の直し）がそのまま受け止め、器が消えた次のコマで戻る。実測：入れ子で読める文字が 461 → 114 文字（確認ダイアログだけ）に減り、確認を閉じると焦点は「削除する」ボタンへ、詳細を閉じると元のカードへ戻る。動きを減らす設定でも同じ |
| D-58 | 2026-07-31 | デザイン | **状態表示の見出しを `h2` にし、本文の1.25倍で組む（最終監査 N3-5）** | 0件・読込失敗・権限なしの3状態で、`h1`（画面の題）→ `h3`（箱の題）と飛んでいた。`.state-box` の見出しを `h2` に改める。あわせて寸法も 16px（本文と同寸）から 20px（本文の1.25倍）に上げた——`h2` にしておいて本文と同じ大きさでは、こんどは見た目と意味が食い違う。PC・スマホ・本人3画面・上司・管理者のすべてで、飛びが無く `h1` がちょうど1つであることを実測した |
| D-59 | 2026-07-31 | デザイン | **見出しの階層を、スマホでも大きさで読み取れるようにする（最終監査 N3-6。D-51 に追記）** | ①`h3` が 13.5〜14px で**本文（16px）より小さかった**（カタチ画面の「企画チーム」、1on1準備メモの ①〜⑤）。`--fs-lead`（17.5px）に上げ、**h1 24 ＞ h2 20 ＞ h3 17.5 ＞ 本文 16** の順に並べ直す。②スマホでは `h1` も `h2` も 20px で、大きさでは区別がつかなかった。**`h2` を下げる道は無い**（20px は本文のちょうど1.25倍で、割ると VD-1 を満たせなくなる）ので、`h1` を 22px に上げる。**ただしアルバムの `h1` だけは 20px のまま**——560px以下では「あなたに届いたありがとう」と「533通」を1行に並べており、21px以上にすると折り返して主役のカードが30px下がる（D-51 の実測。初画面の2枚目が 47px → 19px）。数と1行に置いていない他の画面（カタチ・おくった・チーム・管理者・メンバー詳細）にはこの制約が無いので、そこでは 22px ＞ 20px が成り立つ。**アルバムの通常表示に `h2` は出ないため、同寸で並んで見える場面は無い**（0件・失敗・権限なしの状態表示だけは箱の題が `h2` 20px で並ぶが、箱の中に囲まれていて画面の題とは読み分けられる）。初画面のカードは PC 1280×900 で全体3枚＋一部6枚・1枚目の上端353px、スマホ 390×844 で全体1枚＋2枚目の上端47px・1枚目の上端453px で、**改修の前後で変化なし**（実測） |
| D-60 | 2026-07-31 | 画面 | **節目の帯を「何も無い場所」に置ける下限は 900px ではなく 744px だった（最終監査 N3-3。D-50 に追記）** | D-50 の 900px という区切りは帯の幅（420px）から置いた大まかな線で、右にどれだけ余白が残るかを測っていなかった。そのため 744〜899px（iPad縦810pxなどが入る）では帯が画面の下端へ回り、**画面の縁で切れているカードの本文（`.say`）**に掛かっていた。見出し「あなたに届いたありがとう」は24px×12字で約300px、その右は空いている。帯を 360px に細めれば 744px でも見出しの右に収まる。実測（744 / 760 / 768 / 800 / 810 / 860 / 899）で**文字との重なり0・押せるものの遮り0**。**320px は現状のまま**とする：理由は3つで、①IA-3・RS-4 の対象幅外として D-49・N-07 で整理済み ②320pxでは見出しの右にも下端にも「何も無い場所」が無く、帯を出さない以外の手が無い ③ここだけ第3の置き方（非表示）を作ると、帯の置き場が幅ごとに3通りになり、実装時に取り違えの余地が増える。**744px 未満は D-50 のまま**（下端に貼り付き、重ねてよいのはすでに画面の縁で切れているカードの上端だけ）——390px の実測では掛かるのは2枚目のアバターの丸のみで、本文の文字には掛からない |
| D-61 | 2026-08-01 | デザイン | **拡大詳細の3項目を「台紙の上に重ねた紙」として切り分ける（イシュー#19-4。D-12 の構成は変えない）** | ユーザー指摘「オブリエのメモ、やり取りの流れ、自分のメモの3項目が情報として綺麗に切り分けられてない。のっぺりしている」。原因は3つ重なっていた：①小見出し（`.sheet-sec`）が 12px・不透明度 .75 で、本文より弱かった ②項目のあいだ（18px）と項目の中（8〜16px）の余白がほぼ同じで、余白では塊が読めなかった ③面が1枚しかなく、どこで切れているのかを示すものが何も無かった。**線を足して仕切る道は採らない**——枠線で区切るとたちまち無機質な管理画面になり、原則1に反する。採ったのは**紙を重ねる見立て**で、詳細の地を台紙（`--sheet-board #f7f5f2`）にし、その上に白い紙（`--paper #fffdfa`・角丸14px・やわらかい影）を3枚置く。この2色の関係はアルバムの画面（薄い台紙の上に #fffdfa のカード）と同じなので、詳細だけ別の世界にならない。あわせて**余白の階層**（項目間 20px ＞ 項目内 8px。紙の内側の余白14〜16pxが両側に足されるので、目に映る隔たりは 項目間 約50px ／ 項目内 8〜12px）と、**小見出しの重さ**（`div` → `h3`／12px → `--fs-lead` 17.5px／不透明度をやめて実色。h1 24 ＞ h2 20 ＞ h3 17.5 ＞ 本文 16 の並びは D-59 と同じ物差し）を同時に効かせた。**上司・管理者でも紙は3枚のまま**——3枚目の中身が「じぶんのメモは本人だけのものです」の注記に入れ替わるだけで、枠は消さない（消すと上司ビューだけ組みが変わり、本人の画面そのままという原則7から外れる）。注記が持っていた自前の地色は紙に任せて外した（紙の中にもう一つ箱がある状態になるため）。台紙の上では ✕ の丸（`--navy-bg`）が地に沈むので、詳細のときだけ一段濃い面（`#e9edf4`・文字 4.67:1）にした。**原則8の守り方（`opts.memo` 1本）には触れていない** |
| D-62 | 2026-08-01 | デザイン | **お気に入りを「丸のなかの星」にする（イシュー#19-5。D-11 の置き場を詳細の頭へ）** | ユーザー指摘「お気に入りボタンはまるのなかの星ボタンを」。もとは「★ お気に入り」という横長のボタンで、隣の「Slackで開く」や「削除する」と同じ形をしていた。お気に入りだけは**押すたびに入れ替わる状態**なので、一度きりの行き先を持つボタンとは形から違うほうがよい。置き場は詳細の頭（時刻と ✕ のあいだ）にする——カード表面の★は右上に出る（`.fav-seal`）ので、詳細でも同じ右上にあると同じものだと分かる。同時に、本文の側が「オブリエのメモ／やり取りの流れ／じぶんのメモ」の3枚だけになり、D-61 の切り分けがはっきりする。**状態は色だけに頼らない**：付いていない＝線の星（`fill:none`）＋白い丸、付いている＝塗りの星＋琥珀の丸と輪。塗りか線かは、色が見えなくても・拡大しても・印刷しても分かる。★という**文字は使わない**（書体によって形も太さも変わり「線の星」を作れない）。読み上げには `aria-pressed` と、状態のわかる `aria-label`（「お気に入りに追加する」／「お気に入りから外す」）を持たせ、押すたびに両方を書き換える（`title` も同じ文言にしてマウスの人にも伝える）。**アイコンだけにして名前を失わせない**。当たり判定は PC 32px／スマホ 44px。✕ で使った `background-clip: content-box` の手は使えない（琥珀の輪は `border` なので、内側ではなく当たり判定の外周に描かれてしまう）ので、見える丸を内側の要素（`.fav-dot`）に持たせ、押せる範囲はボタン側の大きさで決めた。**`syncCardMarks()` の構造（★・付箋・読み上げ名を同じ `opts` から同時に作り直す／D-56）には手を入れていない**——上司・管理者ではボタンそのものが描かれないので、`aria-label` にも `title` にも「お気に入り」の語は生成されない（実測0件） |
| D-63 | 2026-08-01 | デザイン | **拡大詳細の右上の日時を読める大きさにする（イシュー#19-1）** | ユーザー指摘「詳細画面における右上日付が文字小さすぎる」。`--fs-note`（12px）を Courier New で組んでいた。Courier は x-height が低く、同じ 12px でも和文ゴシックより一段小さく見える。しかも「たった今 15:58」のように和文を含むため、Courier に無い文字はフォールバックで別の字面になり、行の中で大きさが揃っていなかった。操作面の既定である `--fs-ui`（PC 14.5px／スマホ 15px）に寄せ、書体は本文と同じものに戻す。等幅書体を使っていた理由は数字の桁ぞろえだけだったので、そこは `font-variant-numeric: tabular-nums` で受ける |
| D-64 | 2026-08-01 | 画面 | **「あなたのカタチ」から、説明文と使われない操作を外す（イシュー#18／ユーザー指摘6〜14）** | ユーザーが画面を見ての指摘。外したものは4つ。①**説明文2本**——見出しの下（「…読み解いて分類しています。場面タグは自動で生まれ、増えていきます」）と「感謝された場面」の頭（「タグは現在8つ…バーの長さはいちばん多い場面を100%としたときの割合です…」）。どちらも**画面を見れば分かることを言葉で繰り返していた**（単位の「通」は数のとなりに出ており、バーの長さの基準は並びを見れば読み取れる）。②**「実績として書き出す（STAR形式）」**（F13の一部）。③**「過去のじぶんと重ねる」**（なし／半年前／1年前）。比較用の過去データ（`MY_SHAPE.past`）と、比較の目盛・増減の表示もあわせて外した。**どちらも CLAUDE.md の「今回実装する確定機能」に挙がっていたが、実物を見たユーザーの判断が優先する**（プロトタイプが正）。④**「表で見る」のボタン**（→D-63）。**「小さなありがとう」は指摘12のとおり変えていない**。エクスポート（F13）はアルバムのCSVと年間アルバムPDFが引き続き担う |
| D-65 | 2026-08-01 | 画面 | **ありがとうの推移を累計で出す（イシュー#18-9）** | 月ごと（日ごと）の数ではなく、**いま見ている期間のはじめからの積み上げ**を描く。期間タブを変えると起点も変わる（1年なら1年前から）。元データ（`shape.monthly` / `daily30`）には触れず、描くときに積み上げるだけなので、**件数の出どころが1つという決まり（D-34）は保たれる**（1年の到達点は533通＝アルバムの総数と一致することを実測）。**当月＝集計中の扱いは残す**——累計にすると線は下がらなくなるが、いちばん右がまだ確定していないことは変わらず、累計の最後の点はむしろ「これ以上増えない到達点」に見えてしまう。破線・中空の点・下の注記の3つはそのまま置き、注記の文言だけ「累計にはこの月の途中までしか入っていないので、いちばん右はこれからまだ伸びます」に書き直した。凡例・ツールチップ（「◯月まで」）・表の見出し（「もらった（累計）」）・読み上げ名も累計の言い方にそろえた |
| D-66 | 2026-08-01 | アクセシビリティ | **「表で見る」のボタンは外すが、表そのものは視覚的に隠して残す（イシュー#18-14。D-31 の A-04 対応を維持）** | ユーザーの指摘は「ボタンが要らない」。**ただし単純に消すと A-04（推移の数がホバー限定で、スマホ・キーボード・読み上げから読めない／Critical）が丸ごと戻る**ため、消すのは**入口だけ**にする。表は `position:absolute; width:1px; height:1px; clip-path: inset(50%)` で見た目からだけ外し、`display:none` や `aria-hidden` にはしない——それでは読み上げからも消え、対応が無かったことになる。むしろボタンが無くなったぶん**開く操作が要らなくなり、読み上げには常に届く**。`data-scroll-region` は外した（見えない場所にキーボードの止まり先を作らないため。実測で `tabindex` は付かない）。**指でなぞれば数が出る挙動（`pointerdown` / `pointermove`）は変えていない**（390pxで実測）。表の中身はチャートと同じ数＝累計にそろえてある（D-62） |
| D-67 | 2026-08-01 | 画面 | **場面タグのバーを押せるようにする（イシュー#18-13）** | 押すとアルバムへ移り、**そのタグで絞り込まれた状態**で並ぶ（既存の `#tagFilter` に値を入れて `applyFilters()` を呼ぶ、「よく感謝される場面」のカードと同じ道）。行は `<button>` にしたので**キーボードでも押せる**（当たり判定は PC 25px／スマホ 44px を実測。見た目のバーは太らせず、行の高さと余白だけで確保した）。`button` の中に `div` は置けないため、中身は `span` にして `.bar-fill` だけ `display:block` に戻している。読み上げ名は「（タグ） （数）通。アルバムでこの場面のありがとうを見る」。**タグの並びは場面の分類であって人どうしの順位づけではない**ので、非ランキング原則（原則6）には触れない——**人を並べる形にはしない**。押せるのは本人の画面だけで、上司のメンバー詳細（`allowJump=false`）では今までどおり押せない `div` のまま |
| D-68 | 2026-08-01 | デザイン | **左右のパネルの上端・下端をそろえる（イシュー#18-6）** | ずれの原因は2つあった。①**上端**：積み重ねたパネル用の `.panel + .panel { margin-top: 16px }` が、`.two-col`（横並びの grid）の2枚目にも当たっており、右の箱だけ16px下がっていた。横並びの間隔は grid の `gap` が持っているので、ここでは要らない。②**下端**：`align-items: start` で、それぞれが中身の高さのまま止まっていた。grid の既定（`stretch`）に戻す。実測で上端・下端とも差0px（改修前は上端16px・下端55pxのずれ）。860px以下で1列に落ちたときの間隔は `gap` が持つので変わらない |
| D-69 | 2026-08-01 | 実装 | **判定器は自分の作業コピーを指して走らせる（イシュー#18 の作業メモ）** | `tools/uiux-gate.mjs` と `tools/check.mjs` は既定で共有チェックアウト側のプロトタイプを見る。同じ画面を複数の作業が並行して直しているときは、**必ず自分のワークツリーの絶対パスを引数で渡す**こと。渡し忘れると、直していないファイルに対して「合格」が出る |
| D-70 | 2026-08-01 | デザイン | **「ありがとうの広がり」を、送り主ごとのしゃぼん玉にする（イシュー#20。色・区切り・名前の閾値は #25 の D-76〜D-79 で改訂）** | ユーザー指示：「もらったありがとうの数に応じて相対的に大きさをかえたシャボン玉みたいなもの。ふわふわうかぶモーションとかも入れつつ。大きいシャボンだまには〇〇さんって名前が入っていて、小さいやつには入れない。ホバーすると名前ともらったありがとうの数とかが出てくる」。従来は名札のチップ（アバター＋名前＋「（46通）」）が並ぶだけで、通数は読まないと分からなかった。**大きさの決め方**：面積が通数に比例するように直径は平方根で引く（直径をそのまま通数に比例させると、目には差が誇張される）。ものさしはパネル全体でひとつ——いちばん多い人を1とした比 `--k` を焼き込み、直径＝下限＋（上限－下限）×`--k`。下限46pxは、いちばん小さい玉でも指で押せる44pxを割らないため。上限だけを `@media` で切り替える（PC104px／スマホ92px）ので、幅が変わっても描き直しは要らない。**名前を入れる閾値は `--k` ≥ 0.70**（pxではなく比で決める。pxを境にすると、同じ人がPCでは名前つき・スマホでは名前なし、と入れ替わってしまう）。**非ランキングとの関係**：これは送り主ごとの量の分布であって、本人どうしを並べる順位づけではない（原則6が禁じているのは個人間の順位付け表示）。順位の数字も「トップ」「最多」といった語も出さず、大きさと並びだけで表す（実測：パネルの表示文字と `aria-label` に順位の語0件）。~~**色は人ごとに変えない**（原則3。アバターが濃紺1色なのと同じ理由）——玉はすべて同じ薄い青にする~~ → **D-76 で上書き（カラフルにする）。** 原則3が禁じているのはアバターの人ごとの色分けであって、しゃぼん玉はアバターではない。ユーザーの判断で色を認めたため、この一文は無効。履歴として残す |
| D-71 | 2026-08-01 | 実装 | **浮遊は「画面に入っている間」だけ走らせる（D-25 の轍を踏まない）** | しゃぼん玉の数だけ独立したアニメーションが走るので、そのまま置くと合成の層が枚数ぶん増える。①動かすのは `transform` だけ（幅・位置・影は毎コマ触らない）②`IntersectionObserver` で、広がりのパネルが画面に入っている間だけ `.is-floating` を付ける——カタチ画面は `display:none` で隠れるので、別の画面を見ている間は動きも層も残らない③触れている玉は `animation-play-state: paused` で止める（読んでいるものが動き続けないし、札の位置を測る基準もぶれない）④**`will-change` は書かない**——`transform` のアニメーションが走っている間はブラウザが自分で層に上げるため、書いても層は増減しなかった（実測：あり24／なし24）。効かないものを書き足すと、止めたときに外す約束が1つ増えるだけになる。**実測（1280×900・カタチ画面を開いている間の rAF の間隔、319コマ×3回）**：改修前 p50 16.7ms／p95 16.8ms／20ms超 1.7コマ、改修後 p50 16.7ms／p95 16.7ms／20ms超 0.7コマ（**変化なし**）。**合成の層**：アルバム 0→0、カタチ（広がりが画面外）7→7、カタチ（広がり表示中）7→**24**、アルバムへ戻る 106→106。増えるのは広がりを見ている間だけで、他の画面には持ち越さない。CPUを4倍重くした条件（1990コマ）では p50・p95・p99 とも 16.7〜16.8ms で同じ、20ms超が 5→13コマ（0.25%→0.65%）。**動きを減らす設定**：`prefers-reduced-motion: reduce` で `animation: none`。実測で14秒間の上下の振れ幅が 6.00px → **0.00px**（判定は CSS 側で行うので、設定を途中で変えてもそのとき効く） |
| D-72 | 2026-08-01 | アクセシビリティ | **名前と通数へは、ホバー以外の道からも届くようにする（A-04 の再発防止）** | ホバー限定は、指の人にもキーボードの人にも読めない。①玉そのものを `button` にして Tab で回れるようにし、当たり判定は直径の下限46px（スマホ44px／PC24pxを満たす）②`aria-label` には**必ず名前と通数の両方**を入れる（「上司さんから、113通のありがとう」）——小さい玉には名前を描かないので、描いていないものが読み上げからも落ちると、そこだけ何も伝わらなくなる③焦点が当たったときも札を出す④指で触れる人にはホバーが無いので、押すと札が残り、もう一度押すと引っ込む。札は枠からはみ出さないよう左右を丸めこむ（実測：PC 1280／スマホ 390／744／860 で、玉どうしの重なり0・枠外へのはみ出し0・44px未満0・横スクロール0）。浮きの振れ幅は上下6pxまでで、段の間隔12pxより小さいので、どの幅でも玉どうしは重ならない |
| D-73 | 2026-08-01 | 実装 | **新しく作った部品の名前は、既にある名前とぶつけない（イシュー#20 の差し戻し）** | 取り込みのあと、**上司のメンバー詳細でだけ**しゃぼん玉が `60x22`・`4x4` に潰れ、当たり判定（スマホ44px／PC24px）を割ってゲートが不合格になった。原因は**クラス名の衝突**——`.bub` と `.bub-name` は、**拡大詳細の「やり取りの流れ」の吹き出しが先に使っていた名前**（`.bub { flex: 1 }` / `.bub-name { font-size: var(--fs-cap) }`）で、あとから書いたしゃぼん玉の指定がそのまま吹き出しにも当たっていた。実測：吹き出しは `flex: 1 1 0%` → `0 0 auto`、幅 427px → **433／559／597px**（行幅467pxに対して最大130pxのはみ出し）、話し手の名前は 13.5px → 12px・左寄せ → 中央寄せ・幅は78px止まり。**どちらが勝つかは節を並べた順で決まる**ので、取り込みのしかたによって壊れ方が変わり、「同じ部品なのに描く場所によって効いたり効かなかったりする」ように見えていた（本人のカタチ画面は通り、上司のメンバー詳細では落ちる）。直し方は2つ。①**名前を分ける**：この節で新しく作るものはすべて `spread-` で始める（`.spread-field` `.spread-bub` `.spread-ball` `.spread-name` `.spread-tip` `--sp-min` `--sp-max` `@keyframes spread-float`）。名前がぶつかっている限り、順番を直しても次の取り込みでまた入れ替わるため、順番ではなく名前のほうを直す。②**大きさが崩れても当たり判定を割らない**：`--k` の既定値 `0` を CSS 側に置き、`--sp-min`／`--sp-max` にも `var(…, 46px)` の控えを付けた。焼き込みが失われても `calc()` は壊れず、直径は下限46pxに落ちるだけになる（実測：`--k` を外しても 46px で、24px／44px を割ったものは0件）。JS側でも `--k` を 0〜1 に丸める。**確かめたこと**：吹き出しの幅・名前の大きさ・寄せ・上限幅が、改修前（origin）と1pxも違わないこと（427px／13.5px／start／none）／上司のメンバー詳細の3サブタブ×PC・スマホ×通常・動きを減らす設定の12通りで、しゃぼん玉の大きさが本人のカタチ画面と**完全に同じ文字列**であること |
| D-74 | 2026-08-01 | 画面 | **拡大詳細の操作を、頭の「⋯」と下端の「閉じる」の2か所に置き直す（イシュー#24。D-12 の構成・D-62 の置き場を改める）** | ユーザー指摘4点＋1件。①**スマホで閉じるボタンの位置がおかしい**——640px以下では `.sheet-head` を `flex-wrap` させ、時刻・お気に入り・✕ を `order` で並べ替えていた。そのため折り返すと**お気に入りと ✕ だけが送り主名の直後に割り込み**、日付が次の行に独りで残っていた。直し方は並べ替えをやめて「日付とその右」をひと組（`.sheet-head-right`）にすること。入りきらないときは**組ごと**次の行へ落ちるので、割り込みが起こる形そのものが無くなる。②**閉じるを下端に大きく置き、スクロールしても常に見えるようにする**——1on1準備メモで使っている `.sheet .dlg-actions` の `position: sticky; bottom: 0`（A-16）にそのまま乗せた。`fixed` で浮かせない理由は、`sticky` なら**帯が流れの中に場所を持つ**ため、いちばん下まで送ったときに最後の紙（じぶんのメモ）を覆わないから。地は詳細の台紙（`--sheet-board`）にそろえる（白のままだと台紙の上に白い帯がもう1枚あるように見える）。実測：PC 1280×900 で `x=392 y=806 501×48`、スマホ 390×844 で `x=52 y=746 291×52`。**scrollTop 0 / 半分 / 最下部のいずれでも座標は1pxも動かず、`elementFromPoint` で実際に手前にいることも確認**した。③**上の ✕ を外す**。④**日付の右に3点リーダー**を置き、**お気に入り**と**削除**をそこへ入れる。⑤**「これは感謝ではないと報告する」を廃止**（下記の申し送りあり）。**お気に入りの状態の見せ方**：⋯ の中へ入れると、丸の中の星（D-62）で見えていた状態が畳まれて見えなくなる。採ったのは2本立てで、(i)日付の右に**押せない**星の印（`.fav-mark`／`role="img"`）を、付いているときだけ出す——カード表面の★（`.fav-seal`）と同じ「付いていれば出る／無ければ出ない」関係にそろえたので、一覧と詳細で見え方が食い違わない。押す先を ⋯ ひとつに保ちたいので操作要素にはせず、Tabの止まり先も当たり判定の対象も増やさない。(ii)**メニューの項目名で状態を出す**（「お気に入りから外す」＝いま付いている）。片方だけでは、色や形が見えない人か、メニューを開かない人のどちらかに必ず届かない。**メニューの作法**：`aria-haspopup="menu"` / `aria-expanded`、Enter・Space・↓・↑ で開く（↑ は最後の項目へ）、開いている間は ↑↓ Home End で移動、Escape で閉じて ⋯ へ焦点が戻る、Tab で畳む。Escape はメニューの中で `stopPropagation` して止める——止めないと `document` 側の `onEsc` まで届き、メニューを閉じるつもりで詳細ごと閉じてしまう（A-14 の「Escape はいちばん上のものだけを閉じる」と同じ考え方）。当たり判定は ⋯ が PC 32px／スマホ 44px（丸の見た目は32pxのまま、地色を `background-clip: content-box` で内側だけに塗る）、項目が PC 36px／スマホ 44px。**削除はメニューから直に消さず、いままでどおり確認ダイアログを通す**（原則10）。開く前にメニューを畳んで ⋯ へ焦点を返してから開く——確認の戻り先は開いた瞬間の焦点なので、畳んだあとの見えない項目を戻り先にすると、やめたときに焦点が行き場を失う。**開いたときの初期焦点は器そのもの**（`.f-back` に `data-initial-focus`。`pushModalFocus` の当て先を「[data-initial-focus] → .sheet-close → 最初の操作要素 → 器」の順に改めた。ダイアログは今までどおり ✕ から始まる）。上の ✕ が無くなったので、いままでの当て先をそのまま下の帯にすると**開いた人が最初に聞くのが「閉じる」**になる。器（`role="dialog"`）に当てれば題から読まれ、そのあと Tab は ⋯ → Slackで開く → じぶんのメモ → 保存する → 閉じる、と画面の並び順に進む（実測）。**上司・管理者では ⋯ ごと描かれない**——`opts.fav` も `opts.del` も偽なので、分岐を足さずに既存の opts 1本で決まる（`syncCardMarks()` の構造／D-56 には手を入れていない。実測で上司ビューのカードの `aria-label`・詳細の `aria-label` / `title` / `placeholder` のいずれにも「メモ」「お気に入り」は0件）。**判定器**：`tools/uiux-gate.mjs` の巡回で `[data-del]` が隠れて届かなくなるため、⋯ を開く手順を足し、開いた状態も1場面として測る（D-55 の「測っていない場所は直っていないのと同じ」） |
| D-75 | 2026-08-01 | 仕様 | **誤検出報告（F02）の導線を拡大詳細から外す（イシュー#24。要件・設計側は申し送り）** | ユーザー指摘「感謝ではないと報告するは不要なので消すこと」。プロトタイプからはボタン・確認ダイアログ・処理・`opts.report` のフラグ・`.sheet-danger`（本文の下に「報告する」「削除する」を並べていた帯）まで残さず外した。**外に残っているもの**：①S09(d) 検出品質モニタの「「感謝ではない」の報告 ◯件」と、その下の説明文（`prototype/obrier-prototype-v5.html`）。②要件 F02・F24、基本設計 S05・S09(d)・`DetectionFeedback`・権限表、詳細設計 1.4.3・API `POST /api/cards/{id}/feedback`・バッチ「検出品質集計」・通知文言。**これらは今回触っていない**——今回の指摘は拡大詳細の導線についてのもので、F02 そのものは対応表 No.2 で「対応する」と決めた機能である。**報告する手立てが無いまま件数だけを数え続ける形は残骸になる**ので、次のどちらかをユーザーに決めてもらう必要がある：(a)F02 を機能ごと取り下げ、要件・基本設計・詳細設計から外す（S09(d) は信頼度しきい値の表示だけにする） (b)導線だけを別の場所（例：管理者からの一括見直し）へ移す。**決まるまでは、この矛盾を記録として残す**（CLAUDE.md の「食い違う場合は現行プロトタイプを正とし、ドキュメント側を直す」に従うなら (a) だが、対応表の決定を覆すことになるため独断では行わない） |
| D-76 | 2026-08-01 | デザイン | **しゃぼん玉に色を付ける（イシュー#25-1。D-70 の「色は人ごとに変えない」を改める）** | ユーザー指示「これはカラフルでもいいですよ」。D-70 では**設計原則3（アバターは濃紺1色）に合わせて単色**にしていたが、原則3が禁じているのは**アバターの人ごとの色分け**であって、しゃぼん玉はアバターではない。ユーザーの判断で色を認めるので、D-70 のうち「色は人ごとに変えない（原則3）」の一文を取り下げる（大きさ・非ランキング・読み上げ名の扱いはそのまま生きている）。**色の決め方**：6色を並び順に配るだけで、**色は飾りであって意味を持たない**（人の分類でも部署でも量の段階でもない）ので凡例は置かない。**場面タグの代表7色（`--c-*`）とはぶつけない**——同じ色が別の意味で出ると、タグの色と人の色が混ざって読めなくなる。分け方は色相ではなく**彩度と面積**：タグは「彩度の高い濃色の小さなピル」、しゃぼん玉は「彩度8%前後・明度94%前後の大きな淡い球」で、同じ色相（たとえば桃色）が並んでも別のものとして読める。6色＝#e8eefb 青／#e7f2ec 緑／#f7edf1 桃／#f4f0e4 砂／#e6f1f4 水／#efebf7 藤。地色は単色で置き（その上に中央ほど明るい階調を重ねる）、**6色とも濃紺 #3A4A66 に対して 7.6:1 以上**。玉ごとの縁の色は地色を一段深めたものを対で持つ |
| D-77 | 2026-08-01 | 画面 | **部署の区切りをやめ、ひとつの場に全員を置く（イシュー#25-2・25-6）** | ユーザー指示「企画チームとか営業部とかのくわけは不要です」「説明文章はいらない」。3つの箱（`.map-group`＋`h3`）に分かれていたのをやめ、送り主を名前で束ね直して（同じ人が2つの区切りに出ていたら足し合わせる）ひとつの場に置く。**大きさのものさしも場でひとつ**になるので、区切りをまたいだ比較のねじれが消える。説明文（`.panel-sub`「どこから届いているか…押すか、カーソルを重ねると名前と通数が出ます」）も外した——意味は玉の大きさと、触れたときに出る名前・通数、そして読み上げ名（「◯◯さんから、113通のありがとう」）で伝わる。見出しは `h2`「ありがとうの広がり」だけが残る（`h3` が消えても階層の飛びは出ない） |
| D-78 | 2026-08-01 | 実装 | **大きいものが中央付近、小さいものがその周り（イシュー#25-3）** | ユーザー指示「でかいシャボン玉が真ん中ら辺に集まって、その周りを小さいシャボン玉がふよふよ漂っているイメージ」。**置き方**：場の中心から外へ向かう渦をたどり、**大きいものから順に**空いている場所へ置く（`packSpread`）。先に置いた大きいものが中央を占めるので、小さいものは自然に外へ押し出される。**同心円にしない**：渦に入る角度と半径の伸びを玉ごとに散らす（種は名前から作るので、開くたびに同じ配置になる）。**たまりの形**は横：縦＝広い画面2.4／狭い画面1.55の楕円——真円だと縦に間延びし、場の幅いっぱいに広げると一列に並んでしまう（実際に1096px幅で試して一列になった）。**重なりとはみ出しは置く前に確かめる**（あとから枠内へ丸め込むと、確かめたはずの隙間が消える）。隙間14pxは、浮きの振れ幅6pxと触れたときの拡大（片側4.2px）を足しても触れ合わない量。計算は幅が変わったときだけ（`ResizeObserver`／同じ幅なら何もしない＝高さを書く→また呼ばれる、の堂々巡りを断つ）で、**1回 0.5〜0.7ms**（最大1.4ms）。幅が測れないとき（画面が隠れている間）や詰め切れなかったときは `.is-packed` を付けず、**折り返しの並びのまま出す**（重ならない安全側に倒す）。実測：1280／900／744／640／480／390／360px で**重なり0・はみ出し0・横スクロール0**、いちばん近い玉どうしの隙間 12.9〜13.2px。中心からの平均距離は 60通以上の玉が101px（スマホ82px）に対し5通以下が130px（同179px） |
| D-79 | 2026-08-01 | デザイン | **名前を入れる玉を増やし、触れると少し大きくする（イシュー#25-4・25-5）** | ユーザー指示「文字サイズ小さくしてもいいので、もうちょっと名前入れましょう。極端に小さいやつは名前つけなくていい」「ホバーするとちょっと大きくなる」。**閾値は `--k` ≥ 0.30**（D-70 の 0.70 から下げた。px ではなく比で決めるのは、上限の直径が幅で変わるため——px を境にすると同じ人がPCでは名前つき・スマホでは名前なしと入れ替わる）。0.30 は直径でいうと PC 67.6px／スマホ 64.0px にあたり、4文字の名前が1行で収まる下限。実測：12人中**8人**に名前が入り（29通以上）、入らないのは3〜4通の4人だけ。字は `--fs-note`（PC 12px／スマホ 12.5px）——**判定器の下限が12pxなので、これ以上は小さくできない**。玉の下限を46px→**52px**に上げて名前の居場所を作った（当たり判定44pxは引き続き満たす）。名前が入らない玉も `aria-label` には名前と通数を必ず入れる。**触れたときの拡大は8%**（`transform: scale(1.08)`。直径104pxで片側4.2px＝玉どうしの隙間14pxの内側）で、ホバーだけでなく**キーボードの焦点・指で押したときも同じ見え方**にする。`prefers-reduced-motion: reduce` では**3%**に落とす（合図は残すが、止まっている画面で急に跳ねない）。実測：通常 104px→112.3px（1.08倍）／reduce 104px→107.1px（1.03倍）、浮遊は14秒の振れ幅が通常6.00px・reduce **0.00px**。合成の層は広がりを見ている間だけ 7→20（区切りが1つになったぶん #25 前の24より減った）、フレーム間隔は p50 16.7ms・p95 16.8ms で改修前と変わらず |
| D-83 | 2026-08-01 | 設計原則 | **非ランキング原則の適用範囲を「本人が見る画面」に限定する（原則6を改訂）** | ユーザー指示による（「非ランキングの話は本人に見せるUIUXの話で、上司は別です。上司はしっかり順位も見るべき」）。本人ビュー（アルバム／あなたのカタチ／おくったありがとう）では従来どおり個人間の順位付けを行わない——自己肯定感が低いペルソナに対して比較は逆効果であり、狙い①②を損なうため。一方**上司向けの画面では順位を出してよい**。上司はメンバーの状態を把握するために来ており、順位を隠して遠回りな見せ方をするほうが役に立たない。ただし**順位を出すことと煽ることは別**で、「最下位です」のような書き方はせず淡々と事実を並べる。じぶんのメモの秘匿（原則8）・監視ツールにしない線引き（`signals-for-1on1.md` §5）・静かで温かいトーンは変わらない |

上表の決定にともない、要件定義書・基本設計書・詳細設計書を v1.2 に改版した（D-24〜D-29 は 2026-07-31 の追記で、カードの寸法と文字サイズについては D-19・D-20 の数値を D-28 が上書きしている）。カードデザインの正はB案（紙＋タグ色／D-19）であり、D-1（D案採用）とD-18（A案へ変更）はいずれも履歴として残しているだけで無効である。「対応しない」と判断済みの機能（開封の儀／今日は開ける日／返礼導線／チーム全体量の表示／解析範囲のオプトイン等）を復活させるものではない。

## 追記：2026-08-01 削除した機能の記録とドキュメントv1.3改版（イシュー#26・リリース監査4回目 A4-4）

> リリース監査4回目で、イシュー#22（写真アイコンの検討）とイシュー#23（年間アルバム等の削除）について「decision-log に記録が無い」と指摘された（A4-4）。以下に記録する。イシュー#18（STAR・経年比較の削除）とイシュー#24（誤検出報告導線の削除）はすでに D-64・D-75 で記録済みのため、ここでは繰り返さない。

| No | 日付 | 種別 | 決定 | 内容・理由 |
|---|---|---|---|---|
| D-80 | 2026-08-01 | 提案・判断待ち | **アバターを写真に変える案（イシュー#22）は保留** | ユーザーが「写真のアイコンを追加する」を提案（GitHub #22）。添付画像からアバター用の切り抜きを作る想定だが、**このセッションのプロキシがリポジトリ配下のエンドポイントしか通さないため、`user-attachments` の画像を取得できていない**。画像がリポジトリにコミットされるまで作業を進められない。あわせて、写真化は**設計原則3（アバターは濃紺 #3A4A66 の1色。人ごとの色分けはしない）からの変更**にあたるため、適用前に決めるべき点を4つ整理した：①**どこに使うか**——カード表面のアバター（48px）／拡大詳細のヘッダー／上司のメンバー一覧／やり取りの流れの発言者アイコン。全部か一部か②**本人にも写真を当てるか**——イシュー#21で本人名を一般化しているため、本人だけ無地のままという選択肢もある③**人数が足りない場合**——送り主は6人以上おり、写真素材が5枚では足りない。使い回すか頭文字にフォールバックするか④**書き出しサイズ**——単一HTMLに埋め込むデータURIとして、48px表示なら96px程度（2倍）に抑える。**指定が無ければ「カード・詳細・一覧・やり取りの全部に適用／本人も写真／足りないぶんは頭文字にフォールバック／96px」で進める**方針をイシューに記載済み。プロトタイプは今回変更していない。ドキュメント側もこの保留状態を「未決事項」として記録するに留める（CLAUDE.md 未決事項の節） |
| D-81 | 2026-08-01 | 仕様 | **アルバムから「年間アルバム」「これまでの節目」「1年前の今日」を外す（イシュー#23。プロトタイプ側はコミット `32e899a` で対応済み）** | ユーザー判断による：「アルバムの本人について　年間アルバム、これまでの節目、1年前の自分は全て不要です」。ツールバーのボタン3つに加え、**それぞれのダイアログ・生成処理・起動時の結線・使われなくなった補助関数（`cardYMD` / `refToday`）まで**プロトタイプから削除済み（残骸を残さない）。外したものと元の実装の対応：年間アルバム＝`#btnYear` / `openAnnualDialog()`、これまでの節目＝`#btnPastMs` / `openPastMilestones()`、1年前の今日＝`#btnYearAgo` / `initYearAgo()` / `yearAgoCards()`。**節目そのものは残る**——帯として1件だけ静かに降り、5.5秒で引っ込む形（F09・decision-log内 #10 関連の記述）は変更していない。**ただし「これまでの節目」を外した結果、閉じた節目を後から見に行く手段が無くなった**（`localStorage` で既読は覚えているが、一覧を見返すUIが無い）。帯を見逃すとその節目は二度と出ない。この割り切りでよいかは申し送り事項として残す。**年間アルバム（PDF・F18）は CLAUDE.md の「今回実装する確定機能」に挙がっていたものであり、要件定義・基本設計・詳細設計・CLAUDE.mdのドキュメント側もこの版（v1.3）で実装に合わせて改めた**——F番号は消さず「取りやめ」注記を付ける形にした（イシュー#26の完了条件）。**プロトタイプのコード内コメント・コミットメッセージは本件を「イシュー#22」と記していたが、GitHub上の実際の issue 番号は #23 である**（#22は上記D-80の写真アイコン提案）。プロトタイプは今回変更しないため、コード内コメントの番号違いはそのまま残る。ドキュメント側では正しい番号（#23）で記録する |
| D-82 | 2026-08-01 | 運用 | **要件定義書・基本設計書・詳細設計書を v1.2 → v1.3 に改版し、削除済み機能をF番号ごと「取りやめ」注記に変更、F02は判断待ちとして明記した（イシュー#26）** | 対象：F13のうちSTAR形式書き出し（イシュー#18・D-64）／F18年間アルバムPDF（イシュー#23・D-81）／F20カタチの経年比較（イシュー#18・D-64）／F09のうち「これまでの節目」「1年前の今日」（イシュー#23・D-81）。**いずれもF番号は消さず、取り消し線＋「取りやめ」の注記で履歴として残した**（削除の経緯が後から追えるようにするため）。F02誤検出報告は対応表No.2で「対応する」と決めた機能であり、拡大詳細の導線削除（イシュー#24・D-75）だけでは機能自体を取り下げたことにならないため、**要件・基本設計・詳細設計のすべての該当箇所に「判断待ち」と明記し、(a)機能ごと取り下げ／(b)報告導線を別の場所へ移す、の2択をその都度書いた**。管理者の検出品質モニタ（S09d）の「『感謝ではない』の報告」件数表示は、この判断が付くまで暫定表示のまま残す。**プロトタイプ（`prototype/`）は本改版で一切変更していない**（イシュー#26の指示どおり）ため、`node tools/check.mjs` の結果は改版前後で変わらない |
| D-84 | 2026-08-01 | 画面 | **上司ビューを2階層にした ― チーム全体サマリ（S06）＋メンバー詳細（S07）（イシュー#27）** | ユーザー指示「上司向けの画面はもうちょっと詳細な情報がダッシュボード形式で一覧化できればいい」「上司向けには全体のサマリ画面と、メンバーごとの詳細画面と両方があった方がいい」。**S06 をメンバー一覧の表からチーム全体のダッシュボードへ作り替え**、表そのものは画面のいちばん下に「メンバーごとに見る」＝詳細への入口として残した（表を消すと詳細への導線が無くなるため）。S07 は「本人の3タブをそのまま見せる」構成をやめ、上司が1on1の準備に使う面（この期間のようす／チームの中での関係／1on1で使えるエピソード）を先に置き、そのあとに本人と同じ「カタチ」を続ける構成にした。レポート（1on1準備メモ）はユーザーの言葉どおり従来のまま変えていない |
| D-85 | 2026-08-01 | 画面 | **上司ビューからありがとうカードの一覧を外した（イシュー#27。原則7を改める）** | ユーザー指示「上司向けの導線ですが、カードとか画面で見せなくてもいいかな」。メンバー詳細の「アルバム」「おくった」タブと、そこから開く拡大詳細（A-19で入れたもの）を画面から外した。**個々のカードを開く導線は一切残していない。** 理由は3つ。①1on1で要るのは「引用＋事実＋伝え方例」の3点で、それは準備メモが文章として持っており、同じものを**メンバー詳細に「1on1で使えるエピソード」として常設した**ので、称賛の材料は失われない ②カードの拡大詳細は「やり取りの流れ」（前後の発言）まで開くため、そこまで上司が読めると感謝カードの範囲を超えて会話ログを読む道具に近づく（`docs/signals-for-1on1.md` §5「本人の発言内容そのものの提示はしない」の隣接領域） ③監査ログに残す操作が「サマリ閲覧／メンバー詳細閲覧／1on1準備メモ閲覧」の3つに整理でき、導入企業へ「上司は何を見られるのか」を説明しやすくなる。**これに伴い設計原則7（上司ビュー＝本人が見ている画面そのまま＋上司限定パネル）を「カタチは本人と同じものを見せる／アルバムとおくったは見せない」に改め、CLAUDE.md を書き換えた。** データ（`MEMBER_CARDS` / `MEMBER_SENT`）は残す——`recountShape` がそこから場面タグの通数を作り直しており、消すとカタチの数が推移データと合わなくなるため |
| D-86 | 2026-08-01 | 画面 | **関係のネットワーク図を全体サマリの目玉として新設した（イシュー#27）** | ユーザー指示「メンバー同士がノードで接続されていて、ありがとうが誰から誰によく送られているのかを可視化したい。ノード同士の太さや明るさ、ノード自体の大きさでやり取りの大きさを表現したい」「メンバー内で孤立している人がいないかとかを確認できれば」。**見せ方**：玉の直径＝チームの中でのやり取り（もらった＋おくった）の平方根で 46〜92px、色の濃さも同じ比で連動（多いほど濃い）。線の太さ 1.1〜7.3px と濃さ 0.18〜0.80 は、その向きの通数の平方根に比例。向きは**片道ごとに1本の弧**を左手側へふくらませて描き、終端に線と同じ濃さの三角形を置く（marker では線ごとの濃さを矢じりへ引き継げないため図形で描いた）。相互なら2本の弧がレンズ状に並び、片方向なら1本だけになる。**孤立の見え方**：つながりの少ない人は自然に外側へ押し出され、玉が小さく淡くなる。そのうえで玉の下に「相手の人数・通数」を書き、下の「つながりの数（相手の少ない順）」で誰がどれだけつながっているかを文章でも出す |
| D-87 | 2026-08-01 | 実装 | **ネットワーク図の配置は一度だけ計算して固定する（イシュー#27。D-25・D-71 と同じ考え方）** | 力学モデルを毎フレーム回すと確実に重くなる。**回すのは260回だけで、そこで止めて座標を書き込み、以後は一切動かさない。** 初期位置は円周上＝乱数を使わないので、同じ幅なら毎回まったく同じ形になる（開くたびに配置が変わると、上司は「何かが変わった」と読んでしまう）。やり取りの多い人ほど中央への引きを強くしてあるので、中心にいるのが誰かがそのまま形に出る。計算し直すのは**幅か高さが変わったときだけ**（`ResizeObserver`＋`resize`。同じ寸法で呼ばれたら何もせずに戻る）。**実測（1280×900・Chromium）**：配置計算そのもの 0.4ms／図のDOM書き込み（線44要素＋玉6個）約1ms／上司へ切り替えてから描き終わるまで 16.8ms → 99.4ms（中央値。パネルが6枚増えて上司サマリの要素数が 142 → 543 になったぶん）／**静止時のフレーム時間は 16.53ms → 16.52ms（平均）で変化なし＝走り続けるアニメーションはゼロ**／幅を変えたときの再描画 34ms → 51ms。`prefers-reduced-motion` では玉のトランジションを止める |
| D-88 | 2026-08-01 | アクセシビリティ | **図の情報へは、ホバー以外の道からも全部届くようにした（イシュー#27。A-04 の再発防止・D-72 と同じ作法）** | ①玉そのものを `button` にして Tab で6個すべてを回れるようにし、直径の下限を46pxに固定（スマホ44px／PC24pxを満たす） ②`aria-label` に「相手の人数・おくった／もらった通数・相手ごとの内訳」を全部入れる ③焦点が当たった時点で、図の下の説明欄に同じ内訳が出る（押して固定することもできる。指の人はこちら） ④それとは別に、**全員ぶんの内訳を「つながりの数」として常に文章で出しておく**——図がいちばん読みにくい狭い画面でも、ここだけで用が足りる。SVG 自体は `aria-hidden` にして二重読みを避けている |
| D-89 | 2026-08-01 | 画面 | **関係の変化を、前の同じ長さの期間と比べてインサイトとして出す（イシュー#27）** | ユーザー指示「関係性に変化があれば検出して、インサイトとして表現してくれるといいかもね」。`docs/signals-for-1on1.md` の **B系（広がりの変化）** から4種を実装した：つながりの数の増減（B-1／F-1）／組み合わせが生まれた・途切れた（B-3／F-1）／相互だったものが片方向になった（B-5）／受け取りが1人に寄っている（B-2）。**文言の作法は §4-C にしたがう**——「観測できた事実」「考えられる読み方（〜かもしれません）」「1on1で聞いてみたいこと」の3段に必ず分け、原因を決めつけない。**出す件数は4件まで**（§4-B「5つ並べたら上司は全部読まない」）。同じ人の話が並ばないよう**1人につき2件まで**とし、「つながりの数」を出した人の「途切れた組み合わせ」は同じことの言い換えなので落とす。**気になることばかりにならないよう、＋のサインを必ず1枠とる。** パネルの末尾には §3 の偽陽性管理として「連携が止まっていた期間・長期の休み・編成の変更があると同じように見えます」の注記を常設した |
| D-90 | 2026-08-01 | 仕様 | **メンバー間のやり取りは新しいデータだが、通数は `shape.monthly` から導く（イシュー#27。D-34 を崩さない）** | 新しく持ったのは「その人がおくったぶんを、チームの誰にどれだけ配ったか」という**割合だけ**（`NET_SHARE`。直近の期間ぶんと、その前の期間ぶんの2枚）。辺の通数は **その人のおくった通数（`shape.monthly.sent` の期間合計）× 割合** で作るので、期間を変えれば図の数もメンバー一覧の数と同じように動き、食い違わない。割合の合計は1に満たず、**残りはチーム外へおくったぶん＝図には出ない**。辺は必ず**おくった側から作る**（もらった側からも作ると、同じ組み合わせに2つの数ができる）。そのため図の「もらった」はチームの中で受け取ったぶんだけで、チーム外からのぶんは入らない——画面にもそう書いている |
| D-91 | 2026-08-01 | 画面 | **全体サマリに何を置くか（仮おき。イシュー#27）** | ユーザーの「全体サマリに何を出すべきかは一旦仮おきで検討してください」に対する置き方と根拠。①**チームの数4つ**（届いた通数／うちチームの中で交わされたぶん／やり取りのあった組み合わせ数／新しく生まれた組み合わせ数）＝期間の見当をつけるため。対応表No.21「チーム全体量のゆるやかな表示＝対応しない」は**本人へ共有する話**であり、上司の画面は対象外（`signals-for-1on1.md` §1 も同じ整理） ②**関係のネットワーク図**＝指示の目玉 ③**関係の変化**＝図から読み取ったことを文字にする ④**チームで生まれている場面**（全員ぶんの場面タグを合算）＝チームの仕事がいま何でできているか。人ごとの内訳はメンバー詳細へ ⑤**チーム全体の推移**＝誰か1人が落ちているとき、それが本人の変化か観測側の変化かを切り分けるための基準線（§3・§4-A） ⑥**気にかけたいサイン**＝準備メモにサインが出ている人への近道 ⑦**メンバーごとに見る**＝詳細への入口。**①〜⑦はいずれも仮おきで、実際に上司が使う順に並べ替える余地がある** |
| D-92 | 2026-08-01 | 仕様 | **非ランキングの適用範囲を「本人が見る画面」に限る（イシュー#27。ユーザー指示による原則6の解釈変更）** | ユーザーの言葉「非ランキングの話は本人に見せるUIUXの話で、上司は別です。上司はしっかり順位も見るべき」。**本人ビュー（アルバム／あなたのカタチ／おくったありがとう）は従来どおり順位を出さない。上司ビューでは順位・序列を出してよい**（数値で並べた表・多い順の並び・大きさや色の濃淡による大小の表現）。この判断にもとづき、上司サマリでは**「つながりの数」を相手の少ない順に並べ**（支えが要りそうな人が上に来る）、メンバー一覧に**「つながり」列と並べ替え**を足し、メンバー詳細の相手ごとの内訳を**通数の多い順**にしている。ただし**トーンは変えない**——順位を出すことと煽ることは別で、「最下位」のような言い方はせず事実を淡々と並べる。孤立を見つける目的は支援であって評価ではない（`signals-for-1on1.md` §5 の線引きは維持。深夜・休日労働の個人単位での提示はしない）。**CLAUDE.md の原則6 の書き換えは、依頼元（コーディネーター）側で行う** |
| D-93 | 2026-08-01 | 画面 | **上司の期間タブから「1か月」を外した（イシュー#27）** | 期間は **直近3か月（既定）／直近6か月／直近1年** の3つにした。当月は集計中で月の途中までしか数えておらず、1か月にすると**組み合わせごとの通数がほとんど0になって関係の図が形にならない**（実測：おくった通数がチーム全体で21通、辺はほぼ全滅）。既定を3か月にしたのは、図が形になる最短の期間だから。数の基準の注記には従来どおり「◯月は集計中です」を残している |
| D-94 | 2026-08-01 | 実装 | **判定器の巡回を上司ビューの新しい構造に合わせた（イシュー#27）** | `tools/uiux-gate.mjs` の `visitAll` を2か所直した。①メンバー詳細へ進む行の指定を `#viewTeam .tbl tbody tr` から **`#memberRows tr[data-mi]`** へ。上司サマリにチーム全体の推移チャートを置いたことで、読み上げ用に視覚的に隠してある表（`.chart-table`。これも `.tbl`）が `#viewTeam` の中に増え、`.first()` がそちらに当たって「element is not visible」でメンバー詳細まで一度も進めなくなるため ②メンバー詳細のサブタブ（アルバム／カタチ／おくった）が無くなったので、その巡回を外し、代わりに**関係の図の玉を選んだ状態**を1場面として測るようにした（選択中だけ出る文字と、薄くなった玉を一度も見ないままにしないため）。両判定器の結果は `node tools/uiux-gate.mjs` 29項目すべて○／`node tools/check.mjs` 15項目すべて○ |
| D-95 | 2026-08-01 | 申し送り | **要件定義書・基本設計書・詳細設計書は未改版（イシュー#27）** | 今回の変更は S06／S07 の画面構成そのものを変えている（上司ビューの2階層化・カード一覧の削除・関係のネットワーク図とインサイトの新設・期間タブの変更）。3書のうち少なくとも「画面一覧」「画面設計」「画面詳細」「権限」「API」に反映が要るが、**イシュー#27 の指示は decision-log への追記のみ**だったため v1.3 のまま据え置いた。**次に3書へ触れるときに v1.4 として反映すること**（D-82 と同じ運用） |
| D-96 | 2026-08-01 | デザイン | **アバターを顔写真に変えた（イシュー#22）。設計原則3「アバターは濃紺 #3A4A66 の1色」を書き換えた** | ユーザー判断による。`asset/image_icon.png`（1536x1024・円形に切り抜かれた日本人の顔写真が5枚）から、Playwright + canvas で1枚ずつ切り出して 96px 角（48px表示の2倍）の円形PNGにし、data URI として `prototype/obrier-prototype-v5.html` に埋め込んだ（ImageMagick はこの環境に無いため canvas で切った。円の位置は背景色との差から連結成分で自動検出）。元画像は `asset/` に残している。**適用先は4か所すべて**——カード表面のアバター（48px）／拡大詳細のヘッダー／やり取りの流れの発言者アイコン／上司のメンバー一覧（＋ヘッダー右上の「いま誰として見ているか」）。**送り主は13人いて写真は5枚しかないので、足りないぶんは従来の頭文字（濃紺 #3A4A66 の丸）にフォールバックする**。誰にどの写真を当てるかは `AVA_PHOTOS` の**名前による表引き**で決めており、読み込むたびに変わらない（乱数・出現順・添字には依存させない）。写真が当たるのは 本人（ユーザーさん）・上司さん・佐藤さん・田中さん・鈴木さん の5人で、残り（岡田・高橋・伊藤・渡辺・山本・小林・森・中村・管理者）は頭文字のまま。上司のメンバー一覧に写真と頭文字が混ざって並ぶので、落ちたときの見え方もその画面で確かめられる。**写真が読めなかったときは頭文字に戻る**——頭文字は消さずに写真の下に敷いたままにし、読み込みに失敗した `img` を捕捉の段（capture）で取り除く形にした（data URI をわざと壊して確認済み。JSエラー0・全アバターが頭文字に戻る）。**`alt` は空**にした。アバターが出る4か所はどこも名前が必ず文字で隣に出ており、`alt` に名前を入れると同じ名前を2回読み上げることになるため、写真は装飾として扱う。**白リング（`box-shadow: 0 0 0 5px var(--paper)`）とカード表面の `translateZ(20px)`（原則2）は変えていない**。**「拡大詳細の文字色を濃紺で統一する」部分も原則3に残している**——あれは色の話ではなく文字の話なので、アバターが写真になっても変わらない。ファイルサイズは **写真5枚ぶんの data URI だけで +132,276 バイト（+36%）**（埋め込み直前 376,234 → 直後 508,510）。プロトタイプ全体では 365,839 → 509,932 バイト（+144,093。うち写真が 132,276、残り約11.8KBが下のD-84のCSSと注記）。同じ画をWebP（画質0.9）にすると5枚で25KBに収まる（-107KB）ので、サイズが問題になるならそちらへ差し替えられる。今回は指示どおりPNGにしてある |
| D-97 | 2026-08-01 | デザイン | **「ありがとうの広がり」のしゃぼん玉を、平面の円から球に見える塗りへ変えた（イシュー#22）。WebGL / three.js は使わない** | ユーザー指摘：「しゃぼんって2Dオブジェクトだと思うんだけど、軽量な3Dオブジェクトでもうちょっとリッチにできませんか？」。この画面は**単一HTML・CDN不使用（file:// で開ける）**が制約なので、three.js は使えない。**CSSの塗りだけ**で球に見せた。手がかりは4つ——①左上の光のふくらみ ②右下の影のたまり ③外周だけ濃くした「膜のふち」（`closest-side` で引いているので直径52〜104pxのどれでも輪の太さの割合が変わらない） ④ふちにだけ出る薄膜干渉（虹）。あわせて地色を不透明度86%にして、**方眼の台紙が透けて見える**ようにした。**虹のふちは `mask` ではなく背景の重ね順で作っている**——いちばん下に `conic-gradient` を敷き、その上に「中央は地色・外周は透明」の円板を重ねて、虹をふちにだけ覗かせる。`mask` を使うと玉ごとに別の描画面が要る（12枚ぶん増える）ため避けた。**`filter` / `backdrop-filter` は使っていない**（とくに backdrop-filter は玉が動くたびに背後を取り直すので、12個並べたら落ちる）。`box-shadow` は4本のままで増やしていない。**重さの実測**（1280x900・CPUを4倍遅い端末に見立てる・5回の中央値。写真アバターはどちらの版にも入れた状態で、玉の塗りだけを差し替えて比較）：初回に玉を描くとき Paint 35.0→52.8ms・Raster 131.1→158.8ms。**浮いている5秒間は fps 60.0→60.0 / p95フレーム 16.8→16.8ms / 32ms超のコマ 0→0 / Paint 0→0ms / Raster 0→0ms** で**まったく変わらない**——浮きが `transform` だけの動き（合成側で回る）なので、階調を何枚重ねても1コマは重くならず、増えるのは最初の1回の塗りだけだった。名前の文字（濃紺 #3A4A66）が乗っている**実際の画素**でコントラストを測り直し、最悪 7.54:1 → 6.79:1（基準4.5:1）で通っている。クラス名は `spread-` 始まりのまま（`.bub` は拡大詳細の吹き出しが使っており衝突する）、`prefers-reduced-motion: reduce` で浮きが止まること・当たり判定（PC24px／スマホ44px）・12個すべてキーボードで到達できること・順位を出さないことは、いずれも変えていない |
| D-98 | 2026-08-01 | 運用 | **要件定義書・基本設計書・詳細設計書を v1.3 → v1.4 に改版し、D-95の申し送り（#27の上司ダッシュボード化・原則6の改訂が未反映）を解消した** | D-95で「次に3書へ触れるときに v1.4 として反映すること」としていた作業を実施。**反映1：非ランキング原則（原則6）の適用範囲の明記**——本人ビューは従来どおり非ランキングを維持し、上司・管理者向け画面は順位・数値による並べ替えを表示してよい（D-83・D-92）ことを、要件定義2.基本方針／7.非機能要件、基本設計7.権限設計、詳細設計6.1通知文言方針の該当箇所に明記した。**反映2：上司ビューのダッシュボード化（#27）**——S06をチーム全体サマリ（チームの数・関係のネットワーク図・関係の変化のインサイト・チーム場面タグ・チーム全体推移・気にかけたいサイン・メンバー一覧の7ブロック）、S07をメンバー詳細（上司限定パネル・この期間のようす・チームの中での関係・1on1で使えるエピソード＋本人と同一のカタチ）へ全面改訂し、旧原則7（上司ビュー＝本人と同一画面＋限定パネル）を前提にしていた画面設計・画面遷移・データ設計・権限設計・API・DB・バッチの記述を実装（`prototype/obrier-prototype-v5.html` の`#viewTeam`・`net-*`）に合わせて洗い直した。要件定義にF25（チーム全体サマリ）・F26（関係のネットワーク図・関係の変化）を新設し、F15・F16は改訂として残した（F番号は消していない）。**原則8（じぶんのメモは本人のみ）は不変**であることを、上司向けAPI・データ設計・権限設計の各所に明記した（ネットワーク図・つながり・インサイトもじぶんのメモを集計値・派生指標として使わない）。`prototype/` と `docs/uiux-audit-5.md` は今回変更していない |
| D-99 | 2026-08-01 | デザイン | **やり取りの可視化を「図＋表」の2つ組に組み直した。線から太さ・濃さ・矢じり・弧を全部外し、向きは押したときに流れる点で示す** | ユーザー指摘「矢印で表現するのやめましょう。太さも変えません。矢印がぐにゃぐにゃ曲がってスパゲッティみたいです。気持ち悪い」「ドットがピピピット移動するようなアニメーションとかで表現したい。アニメーションは特定の個人をクリックしたら」。あわせて「一般的にどういう表現技法が使われるか調べ、最適なものを選べ。私がいったことは全部無視してよい。それが優先される」との指示があったため、**先に調べてから決めた**（`docs/network-visualization-study.md`）。**原因**：6人でも有向26本＝密度87%の密なグラフで、そこに1本あたり太さ（1.1〜7.3px）・濃さ（0.18〜0.80）・弧の反り・終端の三角形の**4つの意味を載せて26本重ねていた**。Ghoniem/Fekete(2005) が示すとおり、ノードリンク図は「ノードが多いとき」ではなく「**密なとき**」に破綻する（20ノード程度でも密なら成立しない）ので、人数が少ないから大丈夫という見立てが誤りだった。**採った構成**：(1)**表（隣接行列）＝主**——行が送った人・列が受け取った人・マスの濃さが通数。線を引かないので絡まらず、向きは行と列の役割に内在するため矢印が要らない。一方通行は対角線をまたいだ2マスの比較で、孤立は行と列がまるごと薄い十字の帯で読める。本物の `<table>` なので読み上げとキーボードが構造だけで満たされる（`aria-label` を手で組む必要が消えた）。(2)**図（ノードリンク）＝副**——線は1組1本・直線・太さも濃さも一定で「つながりがある」だけを示す。同実験でノードリンクが唯一勝ち続けたのが経路・つながりの読み取りなので、そこだけを担当させる。**有向26本→15本**（実データでは12本）に減り、これだけで絡まりの大半が消えた。(3)**向きは押したときの点の流れ**——ベクトル場の実験で向きの読み取りは動く点が最も速く正確、**静止した矢印が最下位**だった（外そうとしていたものが最下位で、入れようとしていたものが1位）。**常時は流さない**（26本が同時に動けば動くスパゲッティになる）——組織ネットワーク分析の実務ツールが共通して採るエゴネットワーク表示にならい、押した人のぶんだけ。**作り**は破線の隙送り（`stroke-dasharray` ＋ `stroke-dashoffset`）で1方向につき path 1本、最大10本ほど。通数は点の**間隔**で示し（22〜46px）、**太さは変えない**。おくった `--line-sent`／もらった `--line-recv` を線の左右3.1pxに分けて流す。**見送ったもの**：コード図（CHI2023 で Sankey に完敗。リボンの重なりで追えなくなる失敗の仕方が今のスパゲッティと同型）／Sankey（同じ人が左右に2回出る。7人のチームでは不自然で、孤立が読めない）／エッジバンドリング（束ねると個々が追えず、見た目はむしろ悪化）／BioFabric・ハイブプロット（数万辺の規模のための技法で過剰）。**残したもの**は玉の大きさだけ——指摘は線についてのもので、玉の大小はスパゲッティの原因ではなく、「誰が輪の中心にいるか」を形で示す唯一の手段になる。**細部**：狭い画面では列がはみ出すので表の枠の中だけ横へ送り（頁は動かさない）、名前の列は `position: sticky` で貼り付ける（流れると、どの行を見ているか分からなくなる）。枠に `tabindex="0"` と `role="region"` を付け、キーボードだけでも送れるようにする。列の見出しは幅があるうちは名前をそのまま出し、狭いときだけ頭文字（頭文字だけだと「佐」が誰かを頭で戻す手間が要る）。マスは 44px を割らない。地が濃いマスは字を白にして 4.5:1 を保つ。`prefers-reduced-motion` では点を流さず、向きは表と文章が受け持つので**情報は落ちない** |

## 追記：2026-08-15 アルバムの作り直しと、役割ごとのURL分割

> ユーザー指示（8項目＋URL分割）による。指示は「アルバム画面」に対するもので、上司ビュー・管理者設定は今回触れていない。

| No | 日付 | 種別 | 決定 | 内容・理由 |
|---|---|---|---|---|
| D-100 | 2026-08-15 | 画面構成 | **本人ビューを「アルバム／分析」の2画面にし、もらった・おくったはアルバムの中の切り替えにした** | ユーザー指示「『アルバム、あなたのカタチ、送ったありがとう』からアルバム、分析に変えます。アルバム画面でもらったありがとうとおくったありがとうを切り替えるようにします」。**S03「おくったありがとう」の画面（`#viewSent`）は廃止**し、アルバムのツールバーに `もらった／おくった` の seg を置いた。切り替えで入れ替わるのは見出し・通数・カードの一覧・カードの操作（おくった側はお気に入りと削除を持たない＝従来の `SENT_OPTS` のまま）で、**行き先は変わらない**ので nav ではなく seg（`aria-pressed`）で表す。切り替えると**場面の絞り込みは解除して選択肢を作り直す**——もらった側にしか無いタグを選んだままおくった側へ移ると、0件の面に着いてしまうため。カタチから場面タグで飛ぶ導線（`gotoAlbumWithTag`）は、おくった側を見ていても「もらった」へ戻してから絞る |
| D-101 | 2026-08-15 | 画面詳細 | **カード詳細の「やり取りの流れ」を、頼み→返し→感謝 の3つの抜粋にした** | ユーザー指摘「やりとりの肝になる部分を抜粋して引用してください。やりとりの流れで１つとかしかメッセージが書いてなくて流れが分からない」。**原因**：手書きの12枚には3つのふきだしが入っていたが、推移データから機械で作る残り521枚と、おくった側の全件、上司が開くメンバーのカードは `story` が**感謝のふきだし1つだけ**で、何に対する礼なのかが読めなかった。場面タグごとに「頼み」2通り×「返し」2通りを持たせ（`FILL_FLOW`）、感謝の一言と組んで3つにする。**おくった側は向きが逆**（動いたのは相手、礼を言うのが自分）なので、同じ表から並びを入れ替えて組む。資料品質・情報共有のように**返した中身が物として残る場面だけ**添付を付ける。見出しは「やり取りの流れ（**抜粋**）」とし、全文ではなく肝だけを引いていることを画面に出す |
| D-102 | 2026-08-15 | デザイン | **カードの波紋を、場面タグ色から全カード共通の1色へ変えた** | ユーザー指摘「リップルエフェクトの色をカードごとに変えてるけど、色は１色でいいです。マテリアルデザインガイドラインだと何色なのかな？一般的な色（薄いグレーとか？）にしましょう」。**マテリアルでは波紋は独自の色を持たない**——押下は「その面の on-surface 色を低い不透明度で重ねる」state layer として定義される（M2 のリップルは明るい面で12%、M3 の pressed state layer は10%）。この画面の面は紙（`--paper`）、その上の文字色は `--ink`（#3d3229）なので、**`--ink` を12%で重ねる**＝薄いグレーに見える、が素直な当てはめになる。色の意味づけは上端の帯とタグピルが既に持っており、波紋にも同じ意味を載せる必要は無かった |
| D-103 | 2026-08-15 | 画面詳細 | **「お気に入りのみ」の絞り込みを廃し、お気に入りを先頭へ寄せる形にした** | ユーザー指示「お気に入りのみというボタンも不要。かわりにお気に入りは上位に表示されるようにしましょうか」。絞ると他のカードが**消える**が、寄せるだけなら消えない。並びは安定ソートなので、寄せた組・寄せない組それぞれの中では**選ばれている時系列（新しい順／古い順）のまま**保たれる。**寄せたぶんは月ではなく「お気に入り」の見出しでまとめる**——アルバムは月の見出しで区切っているので、寄せたカードをそのまま月の中に混ぜると「6月」の下に7月のカードが並ぶ形になり、見出しが意味を失う（実際に一度そうなった） |
| D-104 | 2026-08-15 | 機能 | **F13エクスポート（CSV）を取りやめた** | ユーザー判断「csvで書き出す機能は一旦不要」。ツールバーのボタンと `openCsvDialog()` を削除した。**「一旦」なので機能そのものの取り下げではない**——要件定義側はF番号を残したまま「取りやめ」注記を付ける運用（F13 のうちSTAR形式＝D-64 と同じ扱い）。判定器（`tools/uiux-gate.mjs`）が焦点の復帰を測る口としてCSVダイアログを使っていたので、代わりに「カードの操作メニュー」を1つ足した——ここを空にすると測る口が「削除の確認」1つだけになる |
| D-105 | 2026-08-15 | 画面詳細 | **並び順をアイコンだけのボタンにした** | ユーザー指示「並び順について、ボタンはアイコンのみにします。矢印の上下で新しい順か古い順かわかるやつ」。`select`（「並び順：新しい順」）から、矢印＋段の線のアイコン1つを持つ押しボタンへ替えた。**矢印の向きが今の並び**（下向き＝新しい順）で、押すたびに反転する。文字を持たないので、読み上げ名（`aria-label`）とマウス用の説明（`title`）に「いまどちらか」と「押すとどうなるか」を**両方**入れる。当たり判定は PC 24px／スマホ 44px を満たす（`.sort-btn` の規則がスマホ用 `@media` より後ろにあるため、同じ44pxをそちらでも言い直している） |
| D-106 | 2026-08-15 | 画面構成 | **スマホではタブを画面下端に固定し、左右のスワイプでも行き来できるようにした** | ユーザー指示「スマホ画面では、タブ切り替えを画面下部で行えるようにします。なお画面を左右にスワイプにスワイプしても分析とアルバムを切り替えられるようにしたい」。**下端**：`position: fixed` で 640px 以下のときだけ。高さ57px・2つで幅を等分（実測182px×2）・`env(safe-area-inset-bottom)` でホームバーを避ける。`z-index: 30` で**モーダル（60）より下**に敷き、詳細を開いている間は隠れる。本文が隠れないよう `main` の下余白を78pxに増やし、知らせの帯（toast）もタブの上へ出す。**スワイプ**：`touchstart`/`touchend` の差だけで判定し、①指が1本のとき ②横の移動が60pxを超え、かつ縦の移動の1.5倍より大きいとき ③横スクロールする場所（`[data-scroll-region]`＝チャート・表・タブ）から始まっていないとき ④詳細やダイアログが開いていないとき に限る。**縦に読んでいる途中の指のブレで画面が変わらないこと**を実測で確認（横70px・縦400px の払いでは切り替わらない）。タブとキーボードでの行き来は従来どおりなので、この操作が無くても困らない |
| D-107 | 2026-08-15 | 運用 | **本人・上司・管理者を、それぞれ別のURLで配れるようにした** | ユーザー指示「本人、上司、管理者のURLは切り分けましょう」。3つのファイルを手で持つと 576KB の写しが3つになり、直すたびに3か所へ同じ手を入れることになるので、**中身の正は `prototype/obrier-prototype-v5.html` の1本のまま**にした。`tools/build-roles.mjs` が `ROLE_LOCK` の**1行だけ**を差し替えた写しを `prototype/dist/` に作る（`<title>` も「オブリエ アルバム／チームダッシュボード／管理者設定」に分けて、一覧やブラウザのタブで見分けられるようにする）。役割を固定した版では**ヘッダーの役割切り替えを `display:none` で外す**ので、Tab でも止まらない。`prototype/dist/` は生成物なので **gitignore し追跡しない**（commit のたびに576KB×3を積む意味がない。必要なときに作り直す）。**判定器（36項目）は切り替えのある v5 に対して回す**——`dist/` は1行だけ違う機械的な写しで、コントラストや焦点の挙動が変わる余地がないため |

上表にともない `CLAUDE.md` の設計原則6・ファイル構成・実装上の重要仕様を更新した。判定器は**36項目すべて合格**（`node tools/uiux-gate.mjs` 終了コード0）。「おくった側」「古い順に倒した状態」を巡回する場面として追加し、CSVダイアログを外している。**要件定義書・基本設計書・詳細設計書は未改版**——今回の変更は画面一覧（S03の廃止）・画面設計・F13の扱いに反映が要る。**次に3書へ触れるときに v1.5 として反映すること**（D-95・D-98 と同じ運用）。

## 追記：2026-08-15 上司ダッシュボードの言い換えと説明文の削除

| No | 日付 | 種別 | 決定 | 内容・理由 |
|---|---|---|---|---|
| D-108 | 2026-08-15 | 文言 | **チーム全体サマリの説明文を全部外した（7か所）** | ユーザー指示「以下不要です」。外したのは ①上司限定の断り書き（「上司だけが見る画面です。本人には表示されません。じぶんのメモは…」）②数の基準（`teamBasis`。「この画面の数字は直近3か月…つながりは…」）③場面タグの補足④チーム全体の推移の補足⑤得意の補足（`skillSub`。「11種類あり、3人以上が…」）⑥気にかけたいサインの補足⑦メンバー一覧の補足（「行を押すと…」）。**設計原則7の「上司限定部分には『本人には表示されません』と明示する」を、チーム全体サマリについては取り消した**——上司は自分が上司として見ていることを知っている。**メンバー詳細の上司限定パネルには引き続き明示する**（あちらは本人と同じ形の画面の中に上司限定のものが混ざるため、線引きが要る）。**原則8（じぶんのメモは本人のみ）は不変**で、文言を消しただけであり、上司・管理者のAPIレスポンスにメモを含めない実装は変えていない |
| D-109 | 2026-08-15 | 文言 | **並び順の一文は画面から消し、読み上げには残した** | ユーザー指示「並び順：メンバーの名簿のとおり（並べ替えていません）→不要」。要素ごと消すと**並べ替えたことが読み上げに一度も伝わらなくなる**——矢印と `aria-sort` は見出しの中にあり、押した瞬間には読まれない。`#sortState` を `.vh-label`（目には見えないが読み上げには届く）に変え、`role="status"` の生きた領域として残した。目で見ている人には矢印と、押しボタンの `aria-label`（「多い順に並べています。押すと少ない順に…」）が従来どおり伝える |
| D-110 | 2026-08-15 | 文言 | **見出しを3つ言い換えた** | ユーザー指示による。「だれから だれへ ― ありがとうのやり取り」→**関係グラフ**／「チームで生まれている場面」→**チーム全体のありがとうカテゴリ**／「メンバーの得意」→**チームの得意**。3つめは中身（チーム全体で何が厚いか→希少な得意→人ごとの上位5つ）が「チームの」で始まるほうと合っており、人ごとの部分は中の小見出し（人ごとの得意（上位5つ））が受け持つ |
| D-111 | 2026-08-15 | デザイン | **関係グラフの玉に顔写真を入れた** | ユーザー指示「丸のところには顔写真入れる」。カード・拡大詳細・メンバー一覧と**同じ表引き（`AVA_PHOTOS` × `avaKey`）**を使うので、誰がどの写真かは画面をまたいでそろう（D-96）。写真があるのは6人中4人（ユーザー・佐藤・鈴木・田中）で、**残り（高橋・森）は従来の塗りのまま**——名前は玉のすぐ下に必ず出ているので、ここには頭文字を置かない。`.net-ball` に `position: relative; overflow: hidden` を足し、白い縁（`border: 2px solid #fff`）の内側に写真を収めた。**読み込みに失敗した写真は捕捉の段で取り除かれ、そのまま塗りに戻る**（D-96 と同じ仕掛け）。玉の大きさ＝やり取り量、線＝つながりの有無、点の流れ＝向き、という役割分担（D-99）は変えていない |
| D-112 | 2026-08-15 | 操作 | **玉の選択を、玉以外を押したら解除するようにした** | ユーザー指摘「画面外をクリックしたり選択したりすると、ユーザ選択状態が解除されるようにしたい」。これまで解除の手は「もう一度同じ玉を押す」しか無く、選びっぱなしになると**関係の無い玉が薄いまま（`opacity: .3`）の画面に取り残された**。`document` の `pointerdown` を通常の段（capture ではない）で聞き、`.net-node` の中でなければ解除する——通常の段にすることで、玉自身の click（選び直し・同じ玉での取り消し）が先に済んでから判定される。**Escape でも解除する**——キーボードだけの人には押す先の「外」が無いため。ただし詳細やダイアログが開いているときの Escape はそちらが先に受け取る（`.overlay` があるときは何もしない） |

判定器は**36項目すべて合格**（`node tools/uiux-gate.mjs` 終了コード0）。`CLAUDE.md` の設計原則7を D-108 に合わせて書き換えた。**要件定義書・基本設計書・詳細設計書は未改版**のまま（D-107 の申し送りに同じ。次に3書へ触れるときに v1.5 として反映すること）。

## 追記：2026-08-15 カードを名刺比にし、「カタチ」という言い方をやめた

| No | 日付 | 種別 | 決定 | 内容・理由 |
|---|---|---|---|---|
| D-113 | 2026-08-15 | 文言 | **「カタチ」という言い方を全部やめた** | ユーザー指示「『カタチ』という文言が気持ち悪いので全部削除して言い換えてください」。**画面名・タブは「分析」**（`<h1>` は「ありがとうの分析」。副題「もらったありがとうから見えてきた、あなたの得意」はそのまま）、**人の強みを指すときは「得意」**、**場面タグの集計を指すときは「場面」**に置き換えた。置き換えたのは見えている文言だけでなく**ソース中のコメントも全部**（`prototype/obrier-prototype-v5.html` に「カタチ」は0件）。上司のメンバー詳細の「ここから下は、本人が見ている『あなたのカタチ』と同じものです」も「『分析』と同じものです」へ。`CLAUDE.md` も同じ語彙にそろえた。**`docs/decision-log.md` の過去の記述は当時の記録なので書き換えていない**（D-1〜D-112 に残る「カタチ」は履歴であり、いま画面に出ている語ではない）。要件定義・基本設計・詳細設計の3書はまだ「カタチ」を含む（requirements 14件・basic-design 8件・detailed-design 11件）——**v1.5 の改版でまとめて直すこと**（D-107・D-112 の申し送りに同じ） |
| D-114 | 2026-08-15 | デザイン | **ありがとうカードの縦横比を名刺（91×55mm）と同じにした** | ユーザー指示「縦横比を名刺と同じにしたいかも」。`height: 320px` の固定をやめ、`aspect-ratio: 91 / 55` にした。**列数の決め打ちもやめた**——3列/2列/1列を幅で切り替える作りのままだと、900px で3列＝カード幅273px＝**高さ165px** になり中身が入らない。`repeat(auto-fill, minmax(300px, 1fr))` にしてカード1枚の下限から列数を決めるようにしたところ、実測で 1280px→353×214／900px→420×254／560px→534×323／390px→364×220 と、**どの幅でも比が 1.655（＝91/55）ちょうど**で丈も収まった。**「カードの丈は全枚そろえる」（設計原則5）は保たれている**——同じ行に並ぶ札はどれも同じ幅なので同じ丈になる。名刺の丈（いちばん狭いところで214px）に中身を収めるため、アバター48→42px・余白24/22/16→15/20/12px・行間2.05→1.75・本文16→15px、行数は 短い3→2行／中4→3行／長文4→3行に詰めた。**幅が広がったぶん1行に入る字数は増えている**ので、詰めても読める量は落ちていない。判定器の SC 1.4.12（文字間隔を広げても中身が失われない）は30枚中「本文が消えた0枚・枠から出た0枚・横あふれ0px」で通っている |
| D-115 | 2026-08-15 | 画面構成 | **「よく感謝される場面」の面を外した** | ユーザー指示「よく感謝される場面のセクションは不要です（感謝された場面でわかる）」。同じ画面の上にある「感謝された場面」（場面タグのバー）と**同じものを別の見せ方で二度出していた**。使われなくなった `topsHTML` と `.top-*` のCSS（38行）も消した。`shape.tops` のデータは `recountShape` が通数を数え直す先として残している |
| D-116 | 2026-08-15 | 画面詳細 | **今月のレポートに、その月のもらった／おくったと前月比を出した** | ユーザー指示「１ヶ月でもらったありがとうの数とおくったありがとうの数くらいは載せておきましょう（前月比も掲載します、自分の中での比較なら問題ないかと）」。**非ランキング原則（原則6）には触れない**——原則6が禁じるのは個人間の順位付けで、**自分の先月と今月を比べるのはそこに当たらない**（ユーザー判断）。数の出どころは従来どおり `shape.monthly` ひとつ（D-34）で、`monthlyAt()` に前月ぶんを引く `prevMonthLabel()` を足しただけ。**増減に色は付けない**（緑や赤にすると煽りになる。原則11）——記号（＋／−／±）と数だけを淡々と置く。前月のデータが無い月は「前月のぶんはありません」と書く。あわせて見出しを「今月のカタチレポート」→**「今月のレポート」**、その中の「カタチの変化」→**「サマリー」**に変え、説明文（「カタチの変化と、その月を代表する…」）は外した |

判定器は**36項目すべて合格**（`node tools/uiux-gate.mjs` 終了コード0）。

## 追記：2026-08-15 カードデザインの全面刷新（参考デザインに寄せた）

| No | 日付 | 種別 | 決定 | 内容・理由 |
|---|---|---|---|---|
| D-117 | 2026-08-15 | デザイン | **カードを「淡い地＋同系の濃い文字」に全面的に作り替えた（C案）。B案（紙＋タグ色の帯）は終了** | ユーザーが参考画像（レビューカードの並び）を提示し、「割とこのデザイン丸パクリでいいです。これまでのデザインは全部無視してもらっても構いません」との指示。**変えたもの**：①地を白い紙（`--paper`）から**場面タグの淡い同系色**（`--b-*`）へ ②上端の帯（`border-top: 7px`）を廃止——面ぜんたいが色を持つので、帯があると同じことを二度言う ③本文をゴシック中央揃えから**明朝・左寄せ・タグの濃い同系色**（`--t-*`）へ ④アバターを本文の上（中央・48px）から**足もとの右下（28px）**へ ⑤足もとを「タグピル＋日付のゴム印」から**送り主の名前 → 場面タグ → 顔写真の縦積み（右寄せ）**へ ⑥**日付をカードから外した**（ユーザー指示「タイムスタンプはいらないかも」。いつ届いたかは拡大詳細に出る）。**色の作り方**：7つの代表色それぞれに淡い地（`--b-*`）と濃い文字（`--t-*`）を用意し、**全組で 4.5:1 以上**を先に計算してから採用した（実測 4.84 / 6.24 / 5.03 / 5.23 / 5.57 / 5.76 / 5.66）。**印の置き場も動かした**——NEW とメモの印は本文（左寄せ）とぶつかるので左下へ、両方出る札では `.corner-new ~ .memo-dot` で横へ逃がす。お気に入りの★は右上のままだが、`.front:has(.fav-seal) .say` で本文の右をあけて潜り込みを防ぐ。**使わなくなったCSSは消した**（`.tagpill`・`.c-time` とゴム印のかすれマスク一式）。**残したもの**：名刺の縦横比（D-114）・カードはまっすぐ置く・波紋は全カード共通の1色（D-102）・ホバー中の立体チルト（D-30）・アバターの `translateZ`（原則2）・拡大詳細への裏返し（原則4）。**設計原則1を書き換えた**——B案を指す記述（紙・帯・ゴシック中央揃え・タグピル・ゴム印）はすべて無効になり、D-1・D-18・D-19・D-23・D-28 ほかカード見た目の過去の決定はこの版で置き換わっている。判定器は**36項目すべて合格**（コントラスト・SC 1.4.12 とも通過） |
| D-118 | 2026-08-15 | 文言 | **分析画面の副題「もらったありがとうから見えてきた、あなたの得意」を外した** | ユーザー指示「という文言もいらないです」。見出し「ありがとうの分析」の下は、いきなり中身（感謝された場面のバー）から始まる |

判定器は**36項目すべて合格**（`node tools/uiux-gate.mjs` 終了コード0）。

## 追記：2026-08-15 カードの足もとを左寄せにし、スマホのタブを浮かせた

| No | 日付 | 種別 | 決定 | 内容・理由 |
|---|---|---|---|---|
| D-119 | 2026-08-15 | デザイン | **スマホのタブを、下端に貼り付いた帯から「浮いた丸い帯」に変えた** | ユーザー指示「スマホデザインについて、フローティングメニューバーにしてください。デザインは添付した画像に割と忠実に作って欲しい」。画面の端に貼り付けず、**左右と下（14px＋safe-area）に余白を残した白いピル**（全角丸・やわらかい影2枚）にした。1つぶんは**アイコンの下に文字**で、選んでいるものだけ黒く太く、ほかは灰。**参考デザインの薄い灰（#8b8f98）はそのまま使えなかった**——白地に対して 3.24:1 で本文の基準（4.5:1）を割るので **#6b7280（4.83:1）**まで濃くした。選択との差は色の濃さと字の太さで十分つく。アイコンはスマホの帯でだけ出す（PCのヘッダーは文字だけのピルのまま）。**浮かせたぶん、下端に貼り付いていたものを全部持ち上げた**——節目の帯（`.ms-host`）・知らせの帯（`.toast`）を `bottom: 86px + safe-area` へ、本文の下余白を96pxへ。節目の帯は端に貼り付かなくなったので角を4隅とも丸め、下に敷いていたぼかし（`::after`）を消した。**この持ち上げの規則は 899px のブロックより後ろに置くこと**——同じ詳しさなので、前に置くと `bottom: 0` が勝って帯がメニューの裏に潜る（実際に一度潜った） |
| D-120 | 2026-08-15 | デザイン | **カードの足もとを左寄せにし、場面タグをピルにした** | ユーザー指示「ユーザは左揃えです。左にアイコン、右に名前、名前の下にカテゴリ書きます。タグっぽいUIにしてください」。右寄せの縦積み（D-117）から、**左に顔写真（38px）・右に名前・その下に場面タグのピル**という並びに変えた。タグのピルは白を82%で敷き、文字はカードと同じ濃い同系色（`--ct`）——淡いカード地の上でも白地に対する比（5.5〜8:1）がそのまま効く。**NEW とメモの印は右下へ戻した**（左下は足もとが使うようになったため）。カード内の余白も広げた（16/20/14 → 20/22/18px） |
| D-121 | 2026-08-15 | 画面詳細 | **カードで感謝の全文を出すようにし、「…続きは詳細で」を外した** | ユーザー指摘「続きは詳細でのやつ、多分全文表示できるでしょ」。字の大きさを長さで**4段**に刻んだ（短い一言 21px／中くらい 17px／長文 14px／**100字以上 12.5px**。12.5px は付随情報の下限12pxの内側）。実測：**23件中22件が全文で入る**（1280px・390px とも）。900px のように札が広い幅では**23件すべて全文**。入りきらないのは159字の1件だけで、そこは `line-clamp` の「…」が止める。**「…続きは詳細で」の但し書きは外した**——ほとんどの札で出番が無くなり、稀に切れる札は「…」がそれ自体で示す |

判定器は**36項目すべて合格**（`node tools/uiux-gate.mjs` 終了コード0）。途中で判定器が、参考デザインの灰色をそのまま使うとスマホのタブが 3.24:1 になることを捕まえた。
