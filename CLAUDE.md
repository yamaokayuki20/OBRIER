# オブリエ / OBRIER — 実装引き継ぎドキュメント

このリポジトリは、感謝可視化サービス「オブリエ」のプロトタイプと設計ドキュメント一式です。
Claude Codeで実装を進める際は、まずこのファイルを読んでください。

## サービス概要

Slack / Teams のやり取りから感謝の言葉をLLMで自動検出し、受け取った本人の「ありがとうアルバム」に綴じるB2Bサービス。
名称の由来は obrigado（葡：ありがとう）× relier（仏：綴じる・結ぶ）。ともにラテン語 ligare（結ぶ）が語源。

- 狙い①：自己肯定感（届いた感謝を見返せる）
- 狙い②：得意の発見（感謝の場面タグの偏りから自分の得意が見える）
- 狙い③：感謝の連鎖（送る側の記録も残る）

## ファイル構成

| ファイル | 内容 |
|---|---|
| prototype/obrier-prototype-v5.html | **最新プロトタイプ（正）**。本人ビュー＋上司ビュー＋管理者設定（S09）を、確定デザイン（B案：紙＋タグ色の帯／方眼背景）で作った全画面モック。実装の見た目・挙動・文言はこれに準拠する |
| tools/build-roles.mjs | 2つのぶんを書き出す。①**配布用の単一ファイルの写し**——v5 の `ROLE_LOCK` の1行だけを差し替えた本人・上司・管理者ぶんと入口を `prototype/dist/` に（生成物・gitignore 済み・直接編集しないこと）。②**公開用**——リポジトリ直下の `index.html`（入口）と `obrier-person / boss / admin.html`（`?role=` を付けて v5 へ渡すだけの薄い紙）。**中身の正はあくまで v5 の1本**。直下のぶんも生成物なので手で直さず、v5 を直してこれを走らせ直すこと |
| index.html / obrier-*.html（直下） | **GitHub Pages が配る公開ぶん**（`build-roles.mjs` の生成物）。ここだけは commit してある——Pages の配り元がブランチのとき、`dist/` は gitignore してあるので届かないため |
| prototype/obrier-card-simple-v1.html | カードデザイン検討の**過去の記録**。ここで比べているB案／A案／D案は、2026-08-15 の全面刷新（D-117）で**すべて置き換わっている**。いまのカードの正は v5 |
| prototype/obrier-prototype-v4.html | 旧プロトタイプ（**正ではない**）。画面構成・チャート・上司ビューの挙動の参照用。カードデザインと拡大詳細はv5が正 |
| docs/requirements.md | 要件定義 v1.3（F01〜F24、対象外事項、画面一覧S01〜S09、非機能。F13/F18/F20は実装から外れたため「取りやめ」注記あり、F02は判断待ち） |
| docs/basic-design.md | システム構成・画面設計・デザイン方針・データ設計・外部連携・権限 v1.3 |
| docs/detailed-design.md | 画面詳細・遷移仕様・API・DB・検出ロジック・バッチ・通知文言 v1.3 |
| docs/decision-log.md | 意思決定の記録（改善提案対応表＋2026-07の確定事項）。「対応しない」と決めた機能を勝手に実装しないこと |
| docs/source/ | 変換元の .docx / .xlsx 原本（参照用）。内容の正は Markdown 側 |

旧版プロトタイプ（v2 / v3）とダッシュボードモックはこのリポジトリには含めていない。
**ドキュメントと現行プロトタイプで仕様が食い違う場合は、現行プロトタイプ（v5）を正とし、ドキュメント側を直す。**

## 絶対に守る設計原則

1. **カードの世界観（D案：上は白・下だけ淡い同系色）**（D-146。2026-08-21 にユーザーの参考デザインへ全面的に寄せた）：**#F6F6F6 ×薄い青の方眼**を台紙に、**上は白・下だけ場面タグの淡い同系色**という縦のグラデーションを地にしたカード（角丸16px・帯なし）を貼る。`linear-gradient(180deg, #fff 0%, #fff 52%, var(--cb) 82%, var(--cb) 100%)`。感謝本文は**ゴシック（画面と同じ書体）・左寄せ・色を持たない黒 `--card-ink` #2B2B2B**（17px / 1.6 / 500。白地 14.16:1・淡い地 12.0:1 以上）。**色を持つのは足もとだけ**——白いピルの場面タグと、いちばん右の日付が `--t-*`（白地 5.50〜7.33／淡い地 4.84〜6.24）。**足もとは左寄せで、左に顔写真・右に送り主の名前（本名＋ローマ字）・その下に場面タグ（ピル。複数可）、いちばん右に日付**。**印は2か所——NEW は足もとの右・日付の上に右揃え（`.c-end`）、お気に入りの★とメモは右上に一列（`.c-marks`）**（D-151）。カードはまっすぐ置き、傾き・紙のグレイン・ベタ塗り・マスキングテープ・上端の帯・ゴム印は使わない。無機質な管理画面にしない
2. **奥行きはアバターのみ（静止時）**：カード上で3Dの奥行きを持たせるのはアバターだけ（translateZ 20px）。本文・タグ・日付はカードと同じ平面。**ホバー中に限り、控えめな立体チルト（±6.5度）と淡い光沢（強さ.28）を出す**——2026-07にユーザー指示で有効化した（decision-log D-30）。動きは大きくしないこと。カードは静止時はまっすぐ置く
3. **アバターは顔写真、無い人は濃紺 #3A4A66 の頭文字**（イシュー#22 / decision-log D-83）：写真は円形・96px（48px表示の2倍）を data URI で埋め込み、**白リング（`box-shadow: 0 0 0 5px var(--paper)`）は必ず残す**。誰にどの写真を当てるかは**名前からの表引き**で決め、読み込むたびに変わらないようにする。写真が足りない人・読み込めなかった人は**従来どおり濃紺 #3A4A66 の丸に頭文字**（頭文字は消さず写真の下に敷いておき、写真が落ちたらそのまま出る）。人ごとの色分けは相変わらずしない（写真が無い人の丸はすべて同じ濃紺）。写真の `alt` は空にする（名前は必ず隣に文字で出ているため）。**拡大詳細の文字色は同じ濃紺で統一**する——これは色の話ではなく文字の話なので、アバターが写真になっても変えない
4. **カードは押したら拡大詳細**：カードの裏面（めくり）は持たない。遷移は**表にカード・裏に詳細を貼った1つの要素ごと裏返す**（380ms・等速・24コマ、閉じるときは逆再生）。2枚を入れ替える旧方式は二重に見えるため使わない。お気に入りの登録・解除はカード表面ではなく拡大詳細の中で行う（カード表面の★はお気に入り済みの目印のみ）。拡大詳細に出典（Slack/Teams）のタグは置かない
5. **カードの縦横比は名刺（91:55）、寸法はすべて札の幅に対する比（`cqw`）で決める**（D-149）：高さは固定せず `aspect-ratio` で幅から決め、**字も余白もアバターも `cqw`**（参考デザイン 幅501px の比をそのまま写した。本文＝4.8cqw／短い一言＝5.9cqw／長文＝4cqw／100字以上＝3.5cqw／名前＝2.6cqw／タグ＝2.2cqw／日付＝2.4cqw／アバター＝8.8cqw／余白＝5.7cqw）。px で置くと札を広げたぶんだけ字が取り残される。**幅の基準（`container-type: inline-size`）は `.card` / `.wc-card` / `.f-front` の3か所**——基準が無いと `cqw` は画面の幅を指す。3Dの層（`.card3d` / `.face`）には置かないこと（包み込みで preserve-3d が潰れ、アバターの浮きが消える）。列は `repeat(auto-fill, minmax(min(396px, 100%), 1fr))` ＋ `.grid > .card { max-width: 430px }` で、**PCでは3列 400px＝新着の束の札（396px）と同じ大きさ**（D-150）。器の幅は `--page: 1280px`（ヘッダー `.bar`・`main`・知らせの帯 `.ms-host` の3つがこれでそろう）。`min(…, 100%)` と札の上限はどちらも必須（前者が無いと狭い画面で横に溢れ、後者が無いと1列になる幅で札が728pxまで伸びる）。**ほとんどの札は全文が入る**（実測30枚中「…」で止まるのは1枚）。入りきらない稀な札だけ `line-clamp` の「…」で止め、**拡大詳細では省略せず全文を出す**。差は見せ方だけで順位付けはしない
6. **非ランキングは「本人が見る画面」に限る**：本人ビュー（アルバム＝もらった／おくった、および分析）では個人間の順位付け表示をしない。**上司向けの画面は別で、順位を出してよい**——上司はメンバーの状態を把握するのが仕事であり、曖昧にするほうが役に立たない（2026-08 ユーザー指示。decision-log D-83）。ただし順位を出すことと煽ることは別で、静かに事実を並べること
7. **上司ビューは2階層（チーム全体サマリ＋メンバー詳細）**（イシュー#27／decision-log D-83・D-84）。**チーム全体サマリの断り書き（「上司だけが見る画面です／本人には表示されません」）は置かない**（2026-08-15 ユーザー指示。D-108）——上司は自分が上司として見ていることを知っている。**メンバー詳細の上司限定パネルには引き続き明示する**（あちらは本人と同じ形の画面の中に混ざるため）。**ありがとうのカード（一覧・拡大詳細）は上司ビューに置かない**——1on1に要る「引用＋事実＋伝え方例」は準備メモとメンバー詳細の「1on1で使えるエピソード」が文章で持つ。**分析（場面タグ・推移・広がり）だけは本人が見ているものと同じものを見せる**
8. **じぶんのメモは本人のみ**：上司・管理者向けAPIレスポンスに絶対に含めない（管理者も閲覧不可）
9. **場面タグは可変で、1枚に複数付く**（D-149）：固定5分類ではなく、感謝の内容からLLMが自動生成して増える。代表7色（スピード対応/資料品質/調整力/気配り/後輩サポート/アイデア/情報共有）は代表例への色割当であって固定分類ではない。データは `tag`（1つ）でも `tags`（配列）でも書けるが、**読むときは必ず `cardTags(d)` / `mainTag(d)` を通す**。**カードの色は先頭＝いちばん発揮された場面ひとつで決める**（ピルの色も全部それにそろえる。ピルごとに色を変えると主がどれか読めない）。**数えるのは主たる場面だけ**（1通は何場面あっても1通なので、分析の通数＝枚数の約束を崩さない）。**絞り込みはどれかに当たれば出す**（探すための道具なので2つめ・3つめからもたどり着ける）
10. **削除は復元不可**：確認ダイアログ必須
11. **通知は控えめ・言葉調**：週1メールのみ。比較・催促表現の禁止
12. **キザな文言を使わない**：操作フィードバックは「綴じました」ではなく「保存しました」。「綴じる」はサービス名の由来・コンセプト説明にのみ使う
13. **レスポンシブ必須**（PC・スマホ）

## 一般公開（GitHub Pages）

- 公開URLは `https://yamaokayuki20.github.io/OBRIER/`。入口から本人 `/obrier-person.html`・上司 `/obrier-boss.html`・管理者 `/obrier-admin.html` へ行く
- **配り元がどちらでも同じものが出るようにしてある**（D-143）。Settings → Pages → Source が「Deploy from a branch（main）」なら直下がそのまま配られ、「GitHub Actions」なら `.github/workflows/pages.yml` が `_site` に同じ並びを組んで配る。どちらか一方しか効かないので、**片方だけ直すと片方が古いまま出る**
- **直下の `obrier-*.html` は写しではなく、`?role=` を付けて v5 へ渡すだけの薄い紙**（D-144）。v5 は 600KB あり、写しを3つ commit すると版のたびに履歴が太るため。おかげで公開ぶんが v5 より古くなることが構造上ない
- **v5 を直したら `node tools/build-roles.mjs` を走らせ、直下の生成物ごと commit する**。ロゴを差し替えたときも同じ（入口がロゴの data URI を抱えている）

## 実装上の重要仕様（詳細は詳細設計書）

- 感謝検出：ルール前段（辞書・絵文字）→LLM判定（宛先/タグ/皮肉/信頼度）。信頼度0.80以上のみ保存。感謝でないメッセージは保存しない
- 推移チャート：期間タブ（1週間/1か月/1年/3年/5年）、日次または月次、3年以上は横スクロール、ホバーでクロスヘア＋数値。通常時は数値ラベルなし
- 1on1準備メモ（上司向け月次）：①今月のようす②そのまま伝えられる称賛（引用＋事実＋伝え方例）③気にかけたいサイン④聞いてみたい問い⑤前回からの持ち越し
- 上司ダッシュボード（S06。イシュー#27・decision-log D-83〜D-93）：チームの数／**つながりの図と、誰から誰への表の2つ組**（下記）／関係の変化（`docs/signals-for-1on1.md` のB系。事実・読み方・問いの3段で4件まで）／チームで生まれている場面／チーム全体の推移／気にかけたいサイン／メンバー一覧。期間は直近3か月（既定）・6か月・1年
- **やり取りの可視化は「図が形を、表が量と向きを」担う2つ組**（decision-log D-99・選定の根拠は `docs/network-visualization-study.md`）。6人でも密度87%＝密なグラフなので、1本の線に太さ・濃さ・向きを載せると必ずスパゲッティになる（実際になった）。
  - **図（ノードリンク）**：玉の大きさ＝チーム内でのやり取り量。**線は1組につき1本・直線・太さも濃さも一定**で、「つながりがある」ことだけを示す。**矢じり・弧・線の太さ分けは使わない**。配置は一度だけ計算して固定し、幅が変わったときだけ計算し直す
  - **向きは「押したときに流れる点」で示す**。玉を押す（キーボードは焦点）と、その人の線にだけ点が流れる。おくった＝`--line-sent`／もらった＝`--line-recv` の2色を線の左右に分けて流す。通数は点の**間隔**で示す（太さは変えない）。**常時は流さない**——26本が同時に動くと動くスパゲッティになる
  - **表（隣接行列）**：行＝送った人、列＝受け取った人。マスの濃さ＝通数。一方通行は対角線をまたいだ2マスの比較で、孤立は行と列がまるごと薄い十字の帯で読める。本物の `<table>` なので読み上げとキーボードが構造だけで満たされる
  - `prefers-reduced-motion` では点を流さない。向きは表と文章が受け持つので情報は落ちない
- メンバー間のやり取りの通数は、**その人のおくった通数（`shape.monthly.sent`）× 配り先の割合**で作る（D-34「数の出どころは `shape.monthly` ひとつ」を崩さない）
- アラート判定：受信または送信が前月比50%以上減 × 2か月連続。本人には通知しない
- 権限：本人／直属上司／管理者。上司・管理者の閲覧は監査ログに記録
- 管理者設定（S09）：(a)連携設定（Slack/Teams接続・解析対象チャンネル・再連携）(b)チーム/メンバー管理(c)監査ログ(d)検出品質モニタ（誤検出報告件数・信頼度しきい値0.80）
- 今回実装している確定機能：F04絵文字リアクションの計上（分析画面に控えめに。アルバムにはカードを出さない）／F09節目演出（控えめな帯）／今月のレポート
- **本人ビューは2画面**（decision-log D-100）：「アルバム」と「分析」。**もらった／おくったはアルバムの中の切り替え**で、画面（タブ）としては分けない。スマホではタブを**画面下端**に固定し、**左右のスワイプ**でも行き来できる
- **ロゴの正は `asset/logo_obrier.svg`**（D-124）。画面へは data URI にして埋めてある（単一HTML・外部読み込み無しが制約のため）。**ロゴを差し替えたら、そのファイルから作り直してヘッダーの `<img class="brand-logo">` に入れ直すこと**。画像に「OBRIER」「オブリエ」の字が入っているので隣に文字は置かず、名前は `alt="オブリエ"` が持つ
- **版の表示（rev.xx）と自己診断は画面に出さない**（D-125）：版は `window.__rev`、自己診断は **Alt + Shift + D**（状態デモの Alt+Shift+S と同じ流儀）
- **スマホのタブは浮いた丸い帯**（D-119・D-123）：画面の端に貼り付けず、左右と下に余白を残した白いピル。寸法は Apple HIG のタブバーに合わせる（バー58px／1つ46px／アイコン22px／文字12px。ラベルは10ptにできないので12pxに留め、そのぶんアイコンで詰める）。**選んでいるものは紺の地に白**で、地は左右へ滑って移る（`.tab-pill` を実測で置く）。**タブ自身にも同じ紺を敷く**——滑る丸は兄弟要素なので、白い字の下地を機械も支援技術も辿れない。敷くのは丸が着いてから（`transition-delay: .34s`）
- **カードの地は `background-color` を必ず置くこと**（D-148）：グラデーションだけで塗ると `background-color` は transparent のままになり、「この紙は何色の上にあるか」を辿る側（判定器・支援技術・飛ぶ複製）が紙を素通りして後ろの幕まで行く（新着の束で実際に 1.02:1 と出た）。`background-color: #fff` と `background-image: linear-gradient(…)` に分けて書く
- **薄く見せたいときに `opacity` を使わないこと**（D-148。同じ間違いを3度踏んでいる）：透過だと乗る場所によって比が変わる。`--stage-ink-soft` / `--navy-soft` は、どの地でも 4.5:1 を上回るよう**色そのものを決めた**もので、この用途にはそちらを使う
- **カードの色（`--cb` / `--ct`）は `.card` に付くので、飛ぶ複製へは別途渡すこと**（D-122）：複製しているのは `.card3d` だけで、渡さないと `background: var(--cb)` が解決できず**複製が透明になる**（実際に一度なった）
- **`.tab-ico` / `.tab-pill` のように「既定で隠して @media で出す」ものは、隠す規則をスマホ用 `@media` より後ろに置かないこと**（D-123）。同じ詳しさなので隠す側が勝つ。後ろに置くなら、出す側もその後ろで言い直す
- **新着は束で渡す**（D-133）：本人が開いたとき、新しく届いたカードは黙ってアルバムに増やさず、**重ねた束**として先に出す。押しても横へはらっても1枚ずつ送れる（どちらも同じところへ着く）。全部見てから「アルバムに保存する」を押すと、束が散って通数のところへ集まり、綴じられる。**開いた回ごとに一度きり**で、実演用に **Alt + Shift + N** で出し直せる。判定器が「ふだんの画面」を測るための `?wc=0` がある
- **カードの影は触れている間だけ**（D-134）：既定は影なし。浮き（`z`）の進み具合をそのまま影の濃さに使うので、浮くのと同じ速さで現れて一緒に消える
- **カード詳細の「やり取りの流れ」は抜粋**（D-101）：頼み → 返し → 感謝 の3つで、何に対する礼なのかが読めるところまでを引く。全文は載せない
- **カードの波紋は全カード共通の1色**（D-102）：Material の state layer に合わせ、面の on-surface 色（`--ink`）を12%で重ねる。場面タグごとに色を変えない（色は上端の帯とタグピルが既に持っている）
- **お気に入りは絞り込みではなく「先頭へ寄せる」**（D-103）：寄せたぶんは月ではなく「お気に入り」の見出しでまとめる
- **カードデザインは2026-08-21にもう一度作り替えた**（D-146）：ユーザーが持ってきた2枚目の参考デザイン（上は白・下だけ淡い色、本文はゴシックの黒、足もとに名前・タグのピル・日付）に寄せた。**2026-08-15 の版（D-117。淡い地一色＋同系の濃い明朝）は、この版で置き換わっている**。それ以前のカードの決定（D-1・D-18・D-19・D-23・D-28 ほか）も同様
- **カードの日付はデータから組み立てる**（D-146）：データが持つのは表示用の文字列（`6/30 11:04` `たった今 15:58`）だけ。`cardWhen()` が M/D を読めるものだけ月（`2026年 7月`）から年を取って `2026.06.30` に組み、読めないもの（たった今・昨日）はその言葉のまま出す。**無い日付を作らないこと**。時刻はカードには出さない（拡大詳細に出る）
- **「カタチ」という言い方はやめた**（D-113）：画面上の語は **分析**（画面名・タブ）／**得意**（人の強み）／**場面**（場面タグの集計）に置き換えた。新しい文言でこの語を復活させないこと。`docs/decision-log.md` の過去の記述は当時の記録なのでそのまま残している
- **F13エクスポート（CSV）は取りやめ**（D-104。ユーザー判断「一旦不要」）。要件定義側はF番号を残したまま「取りやめ」注記を付ける
- **実物を見たユーザー判断で取りやめたもの**（decision-log D-64・D-75・D-81）：F13のうち「実績として書き出す（STAR形式）」（イシュー#18）／分析の経年比較（イシュー#18）／年間アルバムPDF（イシュー#23）／「これまでの節目」の一覧・「1年前の今日」（イシュー#23）。要件定義側はF番号を残したまま「取りやめ」注記を付けている
- **F02誤検出報告は判断待ち**：拡大詳細の「これは感謝ではないと報告する」導線はイシュー#24で削除済みだが、機能（F02）自体を取り下げるか・報告導線を別の場所へ移すかは未確定（decision-log D-75）。管理者の検出品質モニタに残る「『感謝ではない』の報告」件数表示は、この判断が付くまで暫定表示のまま

## 推奨タスクの進め方

1. プロトタイプv5をブラウザで開き、全画面・全操作を確認する（カードデザインの検討経緯は card-simple-v1 で切り替えて比較できる。正はB案）
2. 技術スタックを提案してユーザーに確認（例：Next.js + TypeScript + Tailwind、モックAPI）
3. まずプロトタイプ同等のフロントエンドを実装（データはモック）→その後API/検出エンジン
4. 変更を加える場合は、docs/decision-log.md の意思決定と矛盾しないか確認する

## 決定済み（2026-07。詳細は docs/decision-log.md の追記節）

- **カードデザインはB案（紙＋タグ色の帯）**。同日中に D案（紙＋マステ）→ A案（ベタ塗り）→ B案 と変わっている（decision-log D-1 → D-18 → D-19）。背景は白地×薄い青の方眼
- 量は行数で受け止める（短い一言／中くらい／長文の3種）
- 絵文字リアクションはカードにしない（分析画面の件数表示のみ）
- カード→拡大詳細の遷移は1つの要素ごと裏返す方式（380ms）
- カードの裏面は廃止。お気に入りの操作は拡大詳細の中へ移動
- 管理者画面（S09 管理者設定）を新設
- 文言はキザな言い回しを避ける

## 未決事項（実装前にユーザーに確認）

- 削除の代替としてアーカイブ（復元可）にするか
- 1on1メモの翌月自動引き継ぎ／カレンダー連携での前日送付（提案中・判断待ち）
- タグの統合・表記ゆれ正規化の運用（自動統合・表記ゆれ正規化・タグ上限）
- 新規タグへの色の割当ルール（規定パレットからの自動割当の可否）
- **F02誤検出報告の扱い**（イシュー#24・#26。decision-log D-75）：(a) F02を機能ごと取り下げ、要件・基本設計・詳細設計から外す（S09(d)検出品質モニタの「感謝ではないの報告」表示も消す） (b) 報告の導線を拡大詳細以外の場所（例：管理者からの一括見直し）へ移す。対応表No.2で「対応する」と決めた機能のため独断では覆さない
- 本番でアバター写真をどこから取るか（イシュー#22で写真に変えた。プロトタイプは `asset/image_icon.png` から切り出した5枚を data URI で持っているが、本番は Slack / Teams のプロフィール画像を取り込むのか・社内名簿から入れるのか・写真を出さない選択を本人ができるのかを決めていない）
